# Work Log 2026-05-29

## 本次目标

根据用户对 `dev/docs/标签字段字典.md` 的反馈，重构媒体来源分类，减少标签重叠与架构冗余，并建立 `other` 候选标签和活字典维护机制。

追加目标：在标签命名空间中加入公司实体标签，用于按大客户、大公司或竞品公司筛选新闻。

追加目标：将现有标签字段字典封装为 OpenClaw 可执行的 `newsletter-tagging` skill，补充流程说明、输出 schema 和校验脚本。

追加目标：基于新的标签字段字典，反推并设计服务 OpenClaw 打标的 SQLite 数据库结构，为后续 RSS JSON 适配和入库脚本做准备。

## 已完成

- 废弃上一版中的 `source_ecosystem_tags` 与 `source_tier` 设计，不再按具体媒体合作伙伴、生态集团或优先级打标签。
- 新增单选字段 `source_nature`，用于判断文章来源性质：官方消息来源、媒体报道/采访、微信公众号、第三方媒体分析、其他。
- 为受控标签字段补充 `other` 使用条件，要求新增标签先进入 `other_candidates`，不得直接污染正式词表。
- 新增每周 qualification 流程：AI 汇总候选标签，人工判断 approved、merged 或 rejected，再由 Codex 回写正式词表和迁移历史标签。
- 强化 AI 打标约束，补充必填字段、证据优先级、置信度阈值、防自由发挥规则和推荐输出顺序。
- 更新 `dev/docs/需求变更日志.md`，新增 004 数据规范变更。
- 更新 `dev/docs/测试记录.md`，新增 T-003 文档检查记录。
- 在 `标签字段字典.md` 中新增 `company_tags` 命名空间，定位为 active company watchlist，而不是永久通用主题词表。
- 新增 7 个 starter 公司标签：雀巢、可口可乐、百事、联合利华、达能、亿滋、嘉吉。
- 新增客户按需公司监测机制和历史重打标流程，示例包含奇华顿、帝斯曼-芬美意、IFF。
- 更新 AI 打标约束，要求公司名只进入 `company_tags`，不得混入 `topic`。
- 更新 `dev/docs/需求变更日志.md`，新增 005 数据规范变更。
- 更新 `dev/docs/测试记录.md`，新增 T-004 文档检查记录。
- 新增 `newsletter-tagging/SKILL.md`，规定 OpenClaw 的文章读取、字段判断、标签输出、Others 处理、证据记录和公司回填流程。
- 将 `dev/docs/标签字段字典.md` 复制到 `newsletter-tagging/references/标签字段字典.md`，作为 skill 的详细词表与定义来源。
- 新增 `newsletter-tagging/schemas/tag_output.schema.json`，约束单篇文章打标结果的 JSON 结构。
- 新增 `newsletter-tagging/scripts/validate_tags.py`，用于校验必填字段、受控标签、证据记录、`other_candidates` 和 active company watchlist。
- 更新 `dev/docs/需求变更日志.md`，新增 006 Agent 能力变更。
- 更新 `dev/docs/系统结构.md`，记录 `newsletter-tagging/` skill 结构。
- 更新 `dev/docs/测试记录.md`，新增 T-005 skill 检查记录。
- 新增 `dev/docs/数据库设计.md`，设计服务 AI 打标的 SQLite schema。
- 明确当前正式应用暂无本项目 SQLite 数据库；`reference/wewe-rss/` 的 schema 可参考 Feed/Article/Run/Log 思路，但不适合直接复用为打标数据库。
- 确定数据库分层：`source_profiles`、`rss_import_runs`、`articles`、`article_contents`、`article_tag_status`、`tagging_runs`、`article_tags`、`tag_evidence`、`candidate_tags`、`other_candidates`、`company_watchlist`、词表版本表与 OpenClaw 读取视图。
- 明确多值标签不写入 `articles`，而是进入 `article_tags`；证据进入 `tag_evidence`；活字典候选进入 `other_candidates`。
- 明确后续 RSS JSON 适配脚本需要输出 `source_key`、`canonical_url`、`published_at`、`fetched_at`、`content`、`raw_tags`、`raw_json` 等字段。
- 更新 `dev/docs/需求变更日志.md`，新增 007 数据库设计变更。
- 更新 `dev/docs/系统结构.md`，记录数据库设计文档和推荐核心表。
- 更新 `dev/docs/测试记录.md`，新增 T-006 数据库设计文档检查记录。

## 验证

- 已检查 `标签字段字典.md` 中媒体来源字段已改为 `source_nature`。
- 已确认字典中包含 `other_candidates`、每周 qualification 流程、入库准则和拒绝准则。
- 已确认字典中包含 `company_tags`、初始公司名单、按需监测名单、历史回填机制和公司实体判定规则。
- 已执行 `python3 -m py_compile newsletter-tagging/scripts/validate_tags.py`，校验脚本语法通过。
- 已用最小合规示例导入并调用 `validate_tags.py` 的校验函数，结果通过。
- 已确认本次仅修改文档规范，未修改应用代码，未运行 RSS 抓取。
- 已对照 `newsletter-tagging/schemas/tag_output.schema.json`，确认数据库设计可承载正式标签、证据、低置信候选、Other 候选和打标审计字段。
- 已检查参考系统 `reference/wewe-rss/apps/server/prisma/schema.prisma`，确认其表结构不足以直接支持当前 AI 打标需求。

## 后续建议

- 下一步可以为 OpenClaw 补充项目批准的本地数据库读取/写回脚本，让 skill 直接调用脚本批量读取待打标文章。
- 下一步可以将 active company watchlist 独立成配置文件，并让校验脚本通过 `--company-watchlist` 在客户项目中加载。
- 后续新增自动打标模块时，应直接读取 `标签字段字典.md` 的约束，避免模型根据媒体名气或合作关系自由生成来源标签。
- 下一步可根据 `dev/docs/数据库设计.md` 编写 SQLite 建表脚本和 RSS JSON 入库适配脚本。
