# 工作日志 - 2026-06-04

**工作阶段：** 禾大字典 v2（字段/标签分离、标签精简、活字典扩展、Watchlist 落库、数据库与数据流梳理）
**执行人：** Claude

---

## 触发：用户对需求报告 / 字典 / 数据库设计的反馈

1. 报告 3.2 新增"重点客户监测"深度分析章节。
2. 字段 vs 标签分离；入库前由脚本判定基础字段；article_tags 只存关联；移除死链/无源/401 等失败字段与 database_query。
3. 数据流梳理；检查并补齐"字典表"与"字典文章关联表"。
4. primary_story_type 多选 + 合并（corporate_move）、删 safety_quality/supply_chain；industry_segment 解释后并入 value_chain 并删除；product_application/functional_claim 的 Others 用活字典；evidence 只留 evidence_text；删 strategic_driver、entity_role；Watchlist 落字典与数据库。

## 完成

| # | 项 | 文件 | 状态 |
|---|----|------|------|
| 1 | 标签字典 v2 重写（3 份同步） | dev/docs/标签字段字典.md、newsletter-tagging/references/标签字段字典.md、outputs/禾大Croda/标签字段字典-禾大美妆个护版.md | Done |
| 2 | schema 更新 | newsletter-tagging/schemas/tag_output.schema.json | Done |
| 3 | validator 更新 | newsletter-tagging/scripts/validate_tags.py | Done |
| 4 | SKILL / prompt / workflow 同步 | newsletter-tagging/SKILL.md、references/llm_prompt.md、references/workflow.md | Done |
| 5 | 数据库设计：新增 taxonomy_terms 字典表、article_tags 精简、tag_evidence 精简、watchlist 规范化、删 database_query/access_status、新增第六节数据流与字段归位 | dev/docs/数据库设计.md | Done |
| 6 | 需求报告：新增重点客户监测、更新业务需求表/字段差异表/Watchlist/§7 | outputs/禾大Croda/禾大Croda-Newsletter需求分析报告.md | Done |
| 7 | 需求变更日志 013（v2.0） | dev/docs/需求变更日志.md | Done |

## 关键决策

| 决策 | 依据 |
|------|------|
| 字段（属性）入库脚本写入、Agent 不补全；article_tags 只存 Tag↔文章关联 | 用户要求区分字段与标签，降低 Agent 负担与后端复杂度 |
| primary_story_type 改多选；business+capital 合并为 corporate_move；safety_quality/supply_chain 拆解删除 | 用户要求合并公司动态、打碎安全质量与供应链贸易 |
| 删除 industry_segment（并入 value_chain）、strategic_driver、entity_role | 用户判定产业大段与价值链重复；战略驱动与实体关系冗余；角色由 Watchlist 匹配 |
| product_application/functional_claim 的 Others 改活字典 other:；ingredient_general 并入 other | 用户要求字典外用"Other:[活字典]"实时打标 |
| evidence 只留 evidence_text | 用户要求去掉 evidence_field 与 reason，仅留客观证据 |
| 新增 taxonomy_terms 字典表 + picklist 机制 | 原设计无字典表；用户要求确认/补齐 picklist 创建机制 |
| Watchlist 落字典第六节 + watchlist_entities（规范化别名、entity_role 为配置项） | 用户要求把已有 Watchlist 定义进字典，规范化欧莱雅/L'Oréal 等 |

## 验证记录

| 检查 | 命令/方式 | 结果 |
|------|-----------|------|
| schema JSON 解析 | `python3 -c json.load` | Passed |
| v2 合规样例（含 other:） | 导入 validate_tags.py 调 validate_item | Passed，errors/warnings 空 |
| v2 负例（已删字段/非法 value_chain/缺证据） | 同上 | Passed，逐条报错 |
| 旧字段残留检索 | `grep` 3 份字典 + skill | 仅保留迁移说明性引用 |

## 待确认

- Watchlist 初始名单与同步频率（客户提供）。

## 备注

本次为字典/schema/validator/数据库/报告的协同结构性修改，未实现实际数据库 batch runner（仍为 I-002 Open）。

---

## 追加会话（Codex）：MVP 月度 JSON 工作流规划

**工作阶段：** MVP 总体规划 + 完整数据库版边界拆分

### 完成

1. 读取最新需求变更日志、2026-06-04 工作日志、问题跟踪、v2 标签字段字典、需求分析报告、数据库设计、项目管理、系统结构、里程碑、newsletter-tagging schema / workflow / prompt / validator。
2. 根据用户新的 MVP 构想，新增 `dev/docs/MVP总体规划.md`：
   - 明确 MVP 采用 `outputs/<month>/` 月度文件夹。
   - 定义 `rss_clean.json`、`tagging.json`、`report_context.json`、`report_content.json`、`report.html`、`run_log.json`。
   - 拆分小龙虾 1（打标 JSON）、小龙虾 2（报告内容 JSON）、HTML 渲染脚本和控制脚本职责。
   - 明确 MVP 与完整数据库版的边界和未来兼容策略。
3. 重写 `dev/docs/项目管理文档.md`，将当前阶段改为 MVP 月度 JSON 工作流规划。
4. 重写 `dev/docs/系统结构.md`，同时保留 MVP 活跃数据流和完整版本数据流。
5. 重写 `dev/docs/里程碑检查清单.md`，将阶段改为 M1-M7：方案冻结、RSS 清洗、打标 JSON、HTML 渲染、报告 JSON、客户验收、数据库版评估。
6. 更新 `README.md`，将下一阶段从数据库优先改为月度 JSON MVP 优先。
7. 更新 `dev/docs/需求变更日志.md` 新增 014 / v2.1。
8. 更新 `dev/docs/问题跟踪.md`：数据库实现降为完整版本保留项，新增 MVP 清洗、报告 Agent、HTML 模板等 open issue。
9. 顺手清理当前执行路径中不再使用的质量评分相关任务表述。

### 关键决策

| 决策 | 依据 |
|------|------|
| MVP 不先做数据库 | 当前客户只需要月度报告，月度 JSON 工件足够支撑交付和复核 |
| 保留数据库设计为第二版 | 后续若需要长期运营、跨月检索、历史重打标，可从 JSON adapter 接入 SQLite |
| 小龙虾 1 / 小龙虾 2 分工 | 打标和报告写作是不同任务，拆开更利于质量控制 |
| HTML 内容由 JSON 配置驱动 | 版式稳定、便于调试，避免 Agent 直接写 HTML |
| 报告阶段读取近 3 个月最终报告 | 只在写作阶段引入历史上下文，避免 MVP 维护存量数据库 |

### 下一步

1. 定义并实现 `rss_clean.json` schema 和清洗脚本。
2. 调整 `newsletter-tagging` workflow，使小龙虾 1 可以直接读取月度 JSON。
3. 定义 `report_content.json` schema，并从 prototype 拆出 HTML/CSS/JS 渲染模板。
4. 设计小龙虾 2 的报告入选规则和 section 输出规范。

---

## 追加会话（Codex）：MVP 项目管理计划与 Agent 指引增强

**工作阶段：** MVP 计划细化 + Agent 开发文档

### 完成

1. 根据用户反馈，重写 `dev/docs/MVP总体规划.md`：
   - 第一部分聚焦项目本身的 MVP 规划，包括定位、边界、交付目标、团队角色、阶段目标、工作量和成功标准。
   - 第二部分聚焦三条团队工作线：项目规划与交付物目标、脚本/HTML 渲染、Agent 与控制脚本。
   - 补充阶段交付物、参与角色、工作量估算、质量门、会议节点和风险应对。
2. 新增 `dev/docs/MVP关键工件契约.md`：
   - 独立定义 `raw/`、`fetch_log.json`、`rss_clean.json`、`tagging.json`、`report_context.json`、`report_content.json`、`report.html`、`run_log.json`。
   - 明确每个工件的生成者、消费者、字段结构、验收规则和未来数据库映射。
3. 新增 `dev/docs/MVP-Agent开发指引.md`：
   - 面向后续 coding agent / openClaw。
   - 说明渐进式阅读顺序、coding agent、小龙虾 1、小龙虾 2、HTML 渲染脚本和控制脚本的边界。
   - 只引用 `dev/docs/MVP关键工件契约.md`，不在指引中展开完整工件契约。
4. 同步 `dev/docs/项目管理文档.md` 与 `dev/docs/系统结构.md`，把新增文档纳入正式文档索引。
5. 更新 `dev/docs/需求变更日志.md` 新增 015 / v2.2。
6. 更新 `dev/docs/问题跟踪.md`，新增并解决 C-012、C-013、C-014。
7. 更新 `dev/docs/测试记录.md` 新增 T-011。
8. 清理过程文档中遇到的旧质量自评字段表述，统一改为证据、规则校验和人工复核口径。

### 关键决策

| 决策 | 依据 |
|------|------|
| MVP 总规划不再承载完整字段契约 | 后续 Agent 需要渐进式读取，契约应独立成文 |
| 团队计划按角色、阶段、工作量和质量门组织 | 用户要求从项目经理管理人的视角让团队保持对齐 |
| Agent 指引只保留执行边界 | 避免把项目计划、客户沟通和完整契约塞进 openClaw 执行文档 |
| 关键工件契约与数据库兼容策略并存 | MVP 当前不做数据库，但未来要能通过 adapter 接入 |

### 下一步

1. 基于 `dev/docs/MVP关键工件契约.md` 实现 `rss_clean.json` 清洗脚本。
2. 冻结第一版 `report_content.json` section 结构。
3. 跑通小龙虾 1 的月度 JSON 打标 runner。
4. 拆出 HTML/CSS/JS 渲染模板并用样例 JSON 生成报告。

---

## 追加会话（Codex）：禾大字典 V2 更新与 V1 存档

**工作阶段：** 标签字段字典 V2 优化 + 输出版本管理

### 完成

1. 按用户要求完成版本管理：
   - 将本次更新前的输出字典移至 `outputs/禾大Croda/Archive/标签字段字典-禾大美妆个护版-V1-存档.md`。
   - 新增客户交付版 `outputs/禾大Croda/标签字段字典-禾大美妆个护版-V2.md`。
2. 将 V2 内容同步到：
   - `dev/docs/标签字段字典.md`
   - `newsletter-tagging/references/标签字段字典.md`
3. 按评估建议更新字典：
   - 新增 `relevance` 准入标准，明确 `relevant`、`not_relevant`、`unclear`。
   - 新增多维协同范式，强调组合查询和各维度边界。
   - `functional_claim` 新增 `anti_glycation`、`blue_light_protection`、`anti_pollution`。
   - `ingredient_technology` 新增 `microfluidics`、`stem_cell`。
   - 明确 `company` 抽取的案例引用边界。
   - 补充 `cross_chain` 判断示例与 `other:<slug>` 归一化规则。
   - 记录不拆分 `corporate_move`、不新增文章体裁字段。
4. 同步执行文档与工具：
   - `newsletter-tagging/SKILL.md`
   - `newsletter-tagging/references/llm_prompt.md`
   - `newsletter-tagging/references/workflow.md`
   - `newsletter-tagging/scripts/validate_tags.py`
5. 更新 `outputs/禾大Croda/禾大Croda-Newsletter需求分析报告.md` 与 `README.md` 中的 V2 文件名引用。
6. 更新 `dev/docs/需求变更日志.md` 新增 016 / v2.3。
7. 更新 `dev/docs/测试记录.md` 新增 T-012。

### 验证记录

| 检查 | 命令/方式 | 结果 |
|------|-----------|------|
| V1 存档与 V2 文件存在 | `ls -la outputs/禾大Croda outputs/禾大Croda/Archive` | Passed |
| 三份 V2 字典一致 | `cmp` 输出字典 vs `dev/docs` / skill reference | Passed，均为 0 |
| schema JSON 解析 | `python3 -c json.load(...)` | Passed |
| validator 语法 | `python3 -m py_compile newsletter-tagging/scripts/validate_tags.py` | Passed |
| 新增标签样例 | 调用 `validate_item`，含新增 5 个标签 | Passed，errors/warnings 均为空 |
| 新增规则检索 | `rg` 检索新增标签、relevance、slug、协同范式 | Passed |

### 剩余风险

- `schema_version` 仍为 `newsletter-tagging/croda-beauty-v1`，本次只更新字典版本与词表；若后续希望 schema 名也升级，需要同步 runner、历史数据和下游消费者。
- 本次只更新字典/skill/validator 与文档引用，未实现新的月度打标 runner。

---

## 追加会话（Codex）：基础字段来源口径补充

**工作阶段：** 字典字段来源澄清

### 完成

1. 根据用户关于 `source_nature` 与基础字段来源的追问，修订 V2 字典 `1.1 基础数据字段`：
   - 新增“RSS/抓取结果是否自带”列。
   - 新增“脚本处理方式（入库前）”列。
   - 新增“备注”列，说明哪些字段依赖 source 配置、哪些由脚本判断、哪些只做格式规范化。
2. 明确 `source_nature` 通常不是 RSS 原始 JSON 自带字段，需由清洗/入库脚本依据 source 配置、域名、公众号来源、发布方与署名规则判断。
3. 明确 `url`、`published_at`、`raw_tags` 等可能来自 RSS，但仍需标准化；`source_key`、稳定 `source_name`、默认 `market_region` 主要来自配置；`fetched_at`、`ingest_method`、`content_language`、`canonical_url`、`extraction_status` 由脚本写入或判断。
4. 同步三份 V2 字典：
   - `outputs/禾大Croda/标签字段字典-禾大美妆个护版-V2.md`
   - `dev/docs/标签字段字典.md`
   - `newsletter-tagging/references/标签字段字典.md`
5. 更新 `dev/docs/需求变更日志.md` 新增 017 / v2.4。
6. 更新 `dev/docs/测试记录.md` 新增 T-013。

### 验证记录

| 检查 | 命令/方式 | 结果 |
|------|-----------|------|
| 三份 V2 字典一致 | `cmp` 输出字典 vs `dev/docs` / skill reference | Passed，均为 0 |
| 关键口径检索 | `rg` 检索 `RSS/抓取结果是否自带`、`source_nature` | Passed |

### 结论

基础字段来源边界已明确，后续清洗脚本可按“RSS 原始字段 / source 配置 / 脚本判断 / 格式规范化”四类处理。

---

## 追加会话（Codex）：媒体源字段设计与 Source Profiles Seed

**工作阶段：** 来源配置字段拆分 + Excel 行业媒体 seed

### 完成

1. 读取 `reference/禾大newsletter/WeChat Article Export.xlsx`：
   - 识别“行业媒体”13 条。
   - 识别其余类别为国内原料商、国际原料商、国内代理商、MNC 客户品牌、国内品牌，更适合进入 `watchlist_entities`。
2. 产出并按用户反馈收窄媒体源字段设计：
   - 不再同时保留 `source_channel` 与 `source_nature_default`。
   - 合并为单一 `source` 字段，用 `web_official`、`wechat_official`、`wechat_domestic_media` 等值表达渠道 + 来源性质。
   - “具体来源”映射为 `source_name`。
   - `Source List` 映射为 `source_list`。
   - 删除 `source_type` 和上一版建议新增的扩展字段。
3. 新增交付物：
   - `outputs/禾大Croda/媒体源字段设计与Seed清单.md`
   - `outputs/禾大Croda/source_profiles_seed.json`
4. 更新 `dev/docs/需求变更日志.md` 新增 018。
5. 更新 `dev/docs/测试记录.md` 新增 T-014。

### 验证记录

| 检查 | 命令/方式 | 结果 |
|------|-----------|------|
| JSON 解析 | `python3 -m json.tool outputs/禾大Croda/source_profiles_seed.json` | Passed |
| seed 数量 | 统计 `source_profiles` | 13 条，与 Excel 行业媒体一致 |
| 字段口径 | 检查 seed JSON key | 已合并为单一 `source`，无被删字段 |

### 剩余风险

- 13 个来源当前未补齐 `wechat_id`、wewe-rss feed 或官网入口，因此状态为 `pending_account_binding`。
- 已在后续会话中解决：V2 字典与 MVP 契约已统一改为轻量 `source` 字段，旧 `source_nature` 不再作为文章基础字段。

---

## 追加会话（Codex）：MVP 基础字段与 media list 派生口径同步

**工作阶段：** 基础字段表完善 + MVP 数据流对齐

### 完成

1. 重新读取 `outputs/禾大Croda/媒体源字段设计与Seed清单.md`、`source_profiles_seed.json`、`dev/docs/MVP总体规划.md`、`dev/docs/MVP关键工件契约.md` 与 Agent 指引。
2. 按用户反馈确认来源字段口径：
   - `source_name/source_key` 先命中 media list / `source_profiles`。
   - 命中后自动派生 `source`、`source_list`、`ingest_method`、默认 `market_region` 和默认 `language/content_language`。
   - `source_nature` 不再作为独立文章基础字段；其业务含义并入 `source`。
3. 更新 V2 字典基础字段表：
   - 新增 `article_id` 作为 `rss_clean.json`、`tagging.json`、`report_content.json` 的主串联键。
   - 新增 `raw_ref` 回溯原始抓取文件和条目。
   - 补齐 `title`、`summary`、`content`、`content_length`、`source`、`source_list`、`extraction_status` 等 MVP 必要基础字段。
4. 同步三份字典：
   - `outputs/禾大Croda/标签字段字典-禾大美妆个护版-V2.md`
   - `dev/docs/标签字段字典.md`
   - `newsletter-tagging/references/标签字段字典.md`
5. 更新 `dev/docs/MVP关键工件契约.md` 的 `rss_clean.json` 示例和约束。
6. 更新 `dev/docs/系统结构.md` 的核心基础字段列表。
7. 更新 `newsletter-tagging/references/llm_prompt.md`、`workflow.md`、`schemas/tag_output.schema.json`、`scripts/validate_tags.py` 中的基础字段说明。
8. 更新 `outputs/禾大Croda/媒体源字段设计与Seed清单.md` 的文章 JSON 示例。
9. 小范围同步 `outputs/禾大Croda/禾大Croda-Newsletter需求分析报告.md` 与 `dev/docs/数据库设计.md` 中的来源字段口径。
10. 更新 `dev/docs/需求变更日志.md`：018 改为 Confirmed，新增 019 / v2.6。
11. 更新 `dev/docs/测试记录.md` 新增 T-015。

### 验证记录

| 检查 | 命令/方式 | 结果 |
|------|-----------|------|
| 三份 V2 字典一致 | `cmp` 输出字典 vs `dev/docs` / skill reference | Passed，均为 0 |
| source seed JSON | `python3 -m json.tool outputs/禾大Croda/source_profiles_seed.json` | Passed |
| tagging schema JSON | `python3 -c json.load(...)` | Passed |
| validator 语法 | `python3 -m py_compile newsletter-tagging/scripts/validate_tags.py` | Passed |
| 旧字段残留 | `rg source_nature` | 活跃运行口径已不依赖；仅历史记录/退场说明保留 |

### 结论

基础字段表已与 MVP 数据流对齐：`raw_ref` 回溯 raw，`article_id` 串联 clean/tag/report，`source_key/source_name` 命中 media list 并派生来源配置字段。

---

## 追加会话（Codex）：Git 清理、提交与推送

**工作阶段：** 本地变更发布

### 完成

1. 确认远端为 `https://github.com/ziyingfay/corda-newsletter.git`，当前分支为 `main`。
2. 清理 Excel 生成的临时锁文件 `reference/禾大newsletter/~$WeChat Article Export.xlsx`。
3. 在 `.gitignore` 中新增 `~$*`，避免 Office 临时文件再次进入未跟踪列表。
4. 将当前正式文档、skill、schema、validator 与输出交付物变更提交并推送到 `origin/main`。

### 验证记录

| 检查 | 命令/方式 | 结果 |
|------|-----------|------|
| Git 状态检查 | `git status --short --branch` | 待提交变更已整理，Excel 临时文件已清理 |
