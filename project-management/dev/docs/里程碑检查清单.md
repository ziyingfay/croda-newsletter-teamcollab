# 里程碑检查清单

> 里程碑完成前，Codex 先自检；需要用户验收时，再请用户确认。

---

## M0 | 项目背景迁移与工作区清理

**目标：** 将项目从食品饮料 newsletter 模板切换为禾大 Croda 定制情报订阅项目。  
**状态：** Completed

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| 客户资料读取 | 已读取 Word prompt、Excel 清单、HTML prototype | Passed | 2026-06-03 完成 |
| 项目身份更新 | README、项目管理、系统结构均以禾大为主 | Passed | 已完成 |
| 字典切换 | `project-management/dev/docs/标签字段字典.md` 为禾大美妆个护版 | Passed | 已同步到 skill references |
| 旧项目归档 | 食品饮料代码和旧字典不在活跃产品/管理文档区 | Passed | 已归档到 `project-management/reference/archive/legacy-food-beverage/` |

## M1 | MVP 方案冻结

**目标：** 确认当前主线为月度 JSON 工作流，完整数据库版保留为第二版设计。  
**状态：** In Progress

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| MVP 总体规划 | `project-management/dev/docs/MVP总体规划.md` 已定义阶段和任务 | Passed | 本轮新增 |
| 两条路线拆分 | MVP 与完整数据库版边界清晰 | Passed | 数据库设计保留 |
| 字段/标签同步 | 规划采用 v2 的 6 个 Agent 标签 | Passed | 删除旧 `industry_segment` 等作为活跃字段 |
| 客户确认项 | 分类、标签、报告标准、HTML 样式待确认 | Pending | 需与客户确认 |

## M2 | RSS 清洗与月度输入

**目标：** 将 RSS/raw 抓取结果清洗为 `product/output/result/spider/202606/<timestamp>-rss-clean.json`。  
**状态：** Not Started

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| 月度目录 | `product/output/result/<component>/202606/` 目录结构确定 | Pending | raw / clean / tagging / report |
| 清洗 schema | `rss_clean.json` 字段契约确定 | Pending | 与基础字段一致 |
| 去重与补字段 | article_id、canonical_url、source、语言、地区等稳定 | Pending | 失败项进 fetch_log |
| 样本运行 | 202606 样本可生成 clean JSON | Pending | |

## M3 | 小龙虾 1 打标 JSON

**目标：** 读取 `rss_clean.json`，输出并校验 `tagging.json`。  
**状态：** Not Started

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| JSON 入口 | `newsletter-tagging` workflow 支持 JSON 文件输入 | Pending | 不依赖数据库 |
| 打标输出 | `product/output/result/tagging/202606/tagging.json` 生成 | Pending | |
| Validator | 打标 JSON 通过 schema/validator | Pending | 使用 v2 字段集 |
| 抽样复核 | 人工检查一批标签和证据 | Pending | |

## M4 | HTML 渲染脚本

**目标：** 固定 HTML 版式，只通过 `report_content.json` 改内容。  
**状态：** Not Started

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| 内容 schema | `report_content.json` schema 定义 | Pending | section / item / chart / source_links |
| 模板拆分 | HTML、CSS、JS 与内容分离 | Pending | 参考 prototype |
| 渲染脚本 | JSON 可渲染成 `report.html` | Pending | |
| 交互调优 | 折叠、筛选、搜索、图表、打印/PDF 友好 | Pending | |

## M5 | 小龙虾 2 报告 JSON

**目标：** 读取过往 3 个月最终报告、当月 RSS JSON 和打标 JSON，生成 `report_content.json`。  
**状态：** Not Started

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| 历史上下文 | `report_context.json` 可生成 | Pending | 近 3 个月报告摘要 |
| 入选规则 | 明确哪些文章写入本期报告 | Pending | 避免重复、关注重要性 |
| 章节写作 | 生成市场快讯、竞品、成分、技术、客户、活动、附录 | Pending | |
| 可渲染 | 输出可被 HTML 脚本渲染 | Pending | |

## M6 | 客户验收与迭代

**目标：** 交付一版 HTML 月报样例，确认分类、标签、内容标准和 HTML 样式。  
**状态：** Not Started

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| 202606 HTML 样例 | 有可打开、可交互的报告 | Pending | |
| 标签反馈 | 客户确认分类和标签 | Pending | |
| 报告反馈 | 客户确认栏目与写作标准 | Pending | |
| 样式反馈 | 客户确认 HTML 版式 | Pending | |
| MVP 流程固化 | 形成月度操作步骤 | Pending | |

## M7 | 完整数据库版评估

**目标：** 在 MVP 跑通后决定是否进入 SQLite 完整版本。  
**状态：** Reserved

| 检查项 | 验收标准 | 状态 | 备注 |
|--------|----------|------|------|
| JSON 导入可行 | MVP JSON 能映射到数据库设计 | Pending | adapter 后续实现 |
| 数据库需求评估 | 客户是否需要跨月查询/长期运营 | Pending | |
| 第二版启动条件 | 明确何时实现 SQLite | Pending | |
