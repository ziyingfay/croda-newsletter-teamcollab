# 需求变更日志

> 本文档是需求、澄清和关键决策的单一事实来源。其他文档如与本文档不一致，以本文档为准，并修正旧文档。

---

## 版本历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-05-28 | 初始化项目工作区 | Codex |
| v1.1 | 2026-05-29 | 重构标签字段字典的媒体来源与活字典机制 | Codex |
| v1.2 | 2026-05-29 | 新增公司实体标签命名空间 | Codex |
| v1.3 | 2026-05-29 | 新增 OpenClaw newsletter-tagging skill | Codex |
| v1.4 | 2026-05-29 | 新增服务 AI 打标的 SQLite 数据库设计 | Codex |
| v1.5 | 2026-06-03 | 将 OpenClaw 打标改为结构化批处理 workflow | Codex |
| v1.6 | 2026-06-03 | 标签字典重构为两层 MECE taxonomy 与公司自由实体抽取 | Codex |
| v1.7 | 2026-06-03 | 新增禾大 Croda 美妆个护客户需求分析与定制标签字典 | Claude |
| v1.8 | 2026-06-03 | 禾大字典：entity_role 与 value_chain_stage 解耦去重；ingredient_technology 内联 other 实时活字典 | Claude |
| v1.9 | 2026-06-03 | 项目从食品饮料模板重整为禾大 Croda 情报订阅 Agent；清理旧残留并同步 Croda 打标 skill | Codex |
| v2.0 | 2026-06-04 | 禾大字典 v2：字段/标签分离、primary_story_type 多选与合并、删除 industry_segment/strategic_driver/entity_role、value_chain 合并、活字典扩展、evidence 精简、Watchlist 落库、新增 taxonomy_terms 字典表、article_tags 精简、数据流与字段归位 | Claude |
| v2.1 | 2026-06-04 | 确认 MVP 采用月度 JSON 工作流，完整数据库设计保留为第二版；新增 MVP 总体规划 | Codex |
| v2.2 | 2026-06-04 | 将 MVP 总体规划拆为项目规划和团队工作计划；新增关键工件契约与 Agent 开发指引 | Codex |
| v2.3 | 2026-06-04 | 禾大字典 V2 优化：V1 存档、补充 relevance 准入、多维协同范式、新增高频功效/技术标签、明确 company/cross_chain/other 边界 | Codex |
| v2.4 | 2026-06-04 | 禾大字典 V2 补充基础字段来源口径：区分 RSS 自带、配置/脚本判断与格式规范化 | Codex |
| v2.5 | 2026-06-04 | 新增媒体源字段设计与 source_profiles seed，合并来源分类为单一 `source` 字段 | Codex |
| v2.6 | 2026-06-04 | 按 MVP 数据流完善基础字段：新增 `article_id`/`raw_ref` 串联关系，`source` 由 media list/source_profiles 派生 | Codex |
| v2.7 | 2026-06-10 | 迁入 Croda Project Management Agent 模板骨架，按 `product/` + `project-management/` 重整目录 | Codex |
| v2.8 | 2026-06-10 | 纠正客户交付 skill 放置规则：产品流程 Xiaolongxia/OpenClaw skills 放入 `product/skills/` | Codex |
| v2.9 | 2026-06-16 | V4 栏目标签收敛为 5 类；行业新闻快讯/热点新闻改为报告 Agent 编排层，不进入打标输出 | Codex |
| v3.0 | 2026-06-18 | 补记 6/13-6/18 本地变更：栏目打标实验、脚本打标层、V4 配置 seed、HTML demo 双版本 renderer 与目录归档 | Codex |
| v3.1 | 2026-06-18 | 新增字典外新词补充机制 PRD：栏目 Agent 仅补 1 个 primary `other:`，只覆盖 ingredient_technology / functional_claim | Codex |
| v3.2 | 2026-06-18 | 补记 V4 脚本配置拆分、`primary_story_type` 弃用，以及 HTML demo 内部审稿 apply_review 流程 | Codex |
| v3.3 | 2026-06-20 | 用“其他-X 待确认”后处理和 HTML 审阅版取代 `other_terms` 方案；新增 extract_json JSON 恢复工具 | Codex |

---

## 变更记录索引

| ID | 日期 | 类型 | 摘要 | 状态 |
|----|------|------|------|------|
| 001 | 2026-05-28 | 建立 | 按 Codex 项目模板建立工作区 | Confirmed |
| 002 | 2026-05-28 | 结构 | 将现有项目文件归位到模板目录 | Confirmed |
| 003 | 2026-05-28 | 数据规范 | 建立字段与标签字典体系 | Confirmed |
| 004 | 2026-05-29 | 数据规范 | 重构媒体来源标签与 Others 维护机制 | Confirmed |
| 005 | 2026-05-29 | 数据规范 | 增加公司实体标签与按需回填机制 | Confirmed |
| 006 | 2026-05-29 | Agent 能力 | 建立 OpenClaw newsletter-tagging 执行型 skill | Confirmed |
| 007 | 2026-05-29 | 数据库设计 | 设计 RSS 入库与 AI 打标 SQLite schema | Confirmed |
| 008 | 2026-06-03 | Agent 能力 | OpenClaw 打标 workflow 结构化与质量自评字段退场 | Confirmed |
| 009 | 2026-06-03 | 数据规范 | 标签字典两层化、MECE 简化与公司自由实体抽取 | Confirmed |
| 010 | 2026-06-03 | 客户交付 | 禾大 Croda 美妆个护 newsletter 需求分析与定制字典 | Confirmed |
| 011 | 2026-06-03 | 数据规范 | 禾大字典去重（entity_role/value_chain_stage）与内联 other 实时活字典 | Confirmed |
| 012 | 2026-06-03 | 项目管理 | 禾大项目工作区重整、旧食品饮料资产归档、Croda 打标 skill 同步 | Confirmed |
| 013 | 2026-06-04 | 数据规范 | 禾大字典 v2：字段/标签分离、标签合并精简、活字典扩展、Watchlist 落库、taxonomy_terms 字典表、数据流梳理 | Confirmed |
| 014 | 2026-06-04 | 项目规划 | MVP 月度 JSON 工作流与完整数据库第二版拆分 | Confirmed |
| 015 | 2026-06-04 | 项目规划 | MVP 计划增强为项目管理视角，新增团队阶段交付、工件契约和 Agent 开发指引 | Confirmed |
| 016 | 2026-06-04 | 数据规范 | 禾大字典 V2 按评估建议更新并存档原版本 | Confirmed |
| 017 | 2026-06-04 | 数据规范 | 明确基础数据字段来自 RSS、来源配置、脚本判断或格式规范化的边界 | Confirmed |
| 018 | 2026-06-04 | 数据规范 | 媒体源字段设计：保留单一 `source` 字段、具体来源和 Source List | Confirmed |
| 019 | 2026-06-04 | 数据规范 | 按 MVP 数据流补全基础数据字段与跨 JSON 串联键 | Confirmed |
| 020 | 2026-06-05 | HTML 配置测试 | 新增 HTML 配置 fixture 与链接导出归档 | Confirmed |
| 021 | 2026-06-05 | 产品需求 / 客户沟通口径 | 确认客户方案口径与 WorkBuddy 使用体验 | Confirmed |
| 022 | 2026-06-10 | 项目结构 | 采用 Croda Project Management Agent 骨架并迁移旧目录 | Confirmed |
| 023 | 2026-06-10 | 项目结构 | 区分内部项目管理 skill 与客户交付产品 skill | Confirmed |
| 024 | 2026-06-13 | 前端 demo / 产品需求 | HTML 月报 demo PRD 对齐 Croda 品牌、KPI、栏目与配置渲染口径 | Confirmed |
| 025 | 2026-06-13 | 产品需求 / 客户确认口径 | HTML 月报 demo PRD 待确认事项答复 | Confirmed |
| 026 | 2026-06-13 | 前端 demo / 配置渲染 | HTML 月报 demo fixture 与静态页面 | Confirmed |
| 027 | 2026-06-13 | 前端 demo / 客户反馈 | 客户可见版 HTML demo 交互与文案收敛 | Confirmed |
| 028 | 2026-06-13 | 前端 demo / 页面排版 | HTML demo 截图反馈版式修复 | Confirmed |
| 029 | 2026-06-13 | 前端 demo / 标签体系 | V4 标签层级、色彩与公司名双语显示 | Confirmed |
| 030 | 2026-06-13 | 前端 demo / 标签体系 | 栏目标签与字典字段标签边界确认 | Confirmed |
| 031 | 2026-06-14 | 前端 demo / 标签筛选交互 | 标签筛选改为 Hover 右展级联菜单 | Confirmed |
| 032 | 2026-06-16 | 数据规范 / 栏目体系 | V4 正式打标栏目收敛为 5 类，行业新闻快讯/热点新闻改为报告编排板块 | Confirmed |
| 033 | 2026-06-18 | 数据处理 / 脚本打标 | 新增配置驱动的 script-tagger 模块 B 与 watchlist / ingredient / event seed | Confirmed |
| 034 | 2026-06-18 | 测试策略 / 栏目打标 | 补记栏目打标实验材料、V4 gold/runs/analysis 与测试文档归位 | Confirmed |
| 035 | 2026-06-18 | 前端 demo / 目录整理 | HTML demo 从运行输出迁入文档评审区，拆成 internal/final 两版 renderer；运行期 report 目录保留为空骨架 | Confirmed |
| 036 | 2026-06-18 | 产品需求 / 标签体系 | 字典外新词补充机制：栏目 Agent 可补 1 个 primary `other:`，`product_application` 不做 other | Confirmed |
| 037 | 2026-06-18 | 数据规范 / 脚本配置 | V4 脚本配置拆分为 ingredient / functional / product / event / watchlist；弃用 `primary_story_type` 脚本标签 | Confirmed |
| 038 | 2026-06-18 | 前端 demo / 内部审稿 | HTML demo 新增 `apply_review.py`，支持确定性 review 修改与 AI Agent 路由边界 | Confirmed |
| 039 | 2026-06-20 | 产品需求 / 审阅流程 | HTML 审阅版支持“其他-X 待确认”、人工补充标签和审阅修改记录；旧 `other_terms` 方案改为历史草案 | Confirmed |
| 040 | 2026-06-20 | 前端 demo / 工具 | 新增 `extract_json.py`，可从渲染 HTML 的内嵌 `#report-data` 无损恢复 `report_content.json` | Confirmed |

---

## 001 | 2026-05-28 | 建立

**类型：** 建立
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

按用户指定脚本创建 `MyNewProject` 工作区：

- `AGENTS.md` 定义 Codex 工作规则
- `dev/docs/` 保存正式过程文档
- `dev/notes/` 保存工作日志和聊天记录
- `reference/` 保存参考资料
- `app/` 保存正式代码
- `outputs/` 保存对外交付物

**原因：**

让项目在多轮对话、多阶段开发中保留上下文、可追溯决策、可复查测试与问题状态。

---

## 002 | 2026-05-28 | 结构

**类型：** 结构
**决策方：** Codex
**状态：** Confirmed

**内容：**

将当前目录中的项目内容按模板归位：

- `TradeMediaRSS/` 移入 `app/TradeMediaRSS/`，作为正式应用代码。
- `wewe-rss/` 移入 `reference/wewe-rss/`，作为参考系统保留。
- `__MACOSX/` 移入 `dev/temp/imported-archive-metadata/`，作为解压残留临时材料。

**原因：**

`TradeMediaRSS` 是当前食品饮料媒体 RSS 抓取系统的核心交付物；`wewe-rss` 是完整 RSS 服务参考项目，包含独立前后端和数据库结构，不宜与当前应用代码混放。

---

## 003 | 2026-05-28 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

基于现有媒体配置、RSS 输出数据、正文样本、媒体清单和参考系统字段，新增 `dev/docs/标签字段字典.md`，作为后续内容分类、LLM 打标、数据库字段扩展和人工校对的词表源头。

初版字典覆盖：

- 媒体源字段、RSS 条目字段、正文抽取字段
- `SourceProfile` 和 `ArticleRecord` 推荐标准对象
- 来源状态、抓取方式、媒体生态、媒体优先级、行业、区域、语言、内容形态、主题、产品品类、配料/技术、功能宣称、产业链、抽取状态等命名空间
- 中文转载稿件标签到标准标签的归一化规则

**原因：**

项目下一阶段计划进入内容分析和标签分类，需要先建立稳定、可追溯、可扩展的字段与标签体系，避免后续自动打标和数据库设计出现同义标签泛滥或字段口径不一致。

---

## 004 | 2026-05-29 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户反馈，重构 `dev/docs/标签字段字典.md`：

- 废弃 `source_ecosystem_tags` 和 `source_tier`，不再按 Fi Europe、Fi Asia、Informa、出版集团或 B2B 媒体等级打标签。
- 新增单选字段 `source_nature`，只判断来源性质：官方消息来源、媒体报道/采访、微信公众号、第三方媒体分析、其他。
- 为各受控标签字段补充 `other` 处理逻辑，但 `other` 不直接扩张正式词表；新标签必须进入 `other_candidates`。
- 明确字典是活字典：每周汇总 `other_candidates`，经人工 qualification 后再决定 approved、merged 或 rejected。
- 增强 AI 打标约束，补充证据优先级、复核规则、定义边界和防自由发挥规则。

**原因：**

上一版媒体来源分类过于具体，存在合作伙伴标签重叠、筛选指导意义不足、媒体优先级不可客观判断等问题。新口径将来源维度收敛为更有策略意义的“来源性质”，并通过候选池和人工审核机制平衡标签体系的灵活性与准确性。

---

## 005 | 2026-05-29 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户反馈，在 `dev/docs/标签字段字典.md` 中新增公司实体命名空间：

- 新增 `company_tags`，用于按公司名筛选新闻，回答“可口可乐最近在做什么”等问题。
- 初始启用 7 个 starter 公司标签：雀巢、可口可乐、百事、联合利华、达能、亿滋、嘉吉。
- 明确公司标签是 active company watchlist，不等同于永久通用词表。
- 支持客户按需启用竞品或目标客户名单，如奇华顿、帝斯曼-芬美意、IFF。
- 新增历史重打标机制：新增公司名单后，对历史文章按别名匹配、上下文校验并回填 `company_tags`。
- 更新 AI 打标约束，禁止把公司名当成主题标签，要求公司必须是文章主体或重要相关方。

**原因：**

Newsletter 后续需要支持按大客户、大公司和竞品公司筛选行业动态。公司名属于实体监测维度，既不能混入主题标签，也不适合通过 `other` 机制自然扩张，因此需要单独的命名空间和按需回填机制。

---

## 006 | 2026-05-29 | Agent 能力

**类型：** Agent 能力
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

新增 `newsletter-tagging/` skill，用于让 OpenClaw 稳定执行“读取文章 → 判断字段 → 输出标签 → 处理 Others → 记录证据 → 支持公司回填”的重复任务。

skill 结构包括：

- `product/skills/newsletter-tagging/SKILL.md`：精简执行流程、输出要求、数据库任务边界。
- `product/skills/newsletter-tagging/references/标签字段字典.md`：复制现有标签字段字典作为详细词表与定义来源。
- `product/skills/newsletter-tagging/schemas/tag_output.schema.json`：定义打标输出 JSON 结构。
- `product/skills/newsletter-tagging/scripts/validate_tags.py`：无第三方依赖的输出校验脚本，覆盖必填字段、受控词表、证据记录、`other_candidates` 和 active company watchlist 校验。

数据库读取脚本本次暂不实现。skill 规定：如果项目已有数据库读取/写回脚本，OpenClaw 应调用现成脚本；如果没有，应先产出输入/输出契约并请求确认，不在打标任务中临时重写数据库访问逻辑。

**原因：**

现有 `标签字段字典.md` 是高质量知识源，但不足以让 Agent 稳定执行批量打标。新增 skill 将词表知识、执行流程和 JSON 校验约束分层，避免 Agent 只“参考”字典而缺少可复现流程。

---

## 007 | 2026-05-29 | 数据库设计

**类型：** 数据库设计
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户要求，基于新标签字典反推 SQLite 数据库结构，新增 `dev/docs/数据库设计.md`。

核心设计决策：

- 不直接复用 `reference/wewe-rss/` 的 Feed/Article schema；该参考结构适合 RSS 服务展示，但缺少 AI 打标需要的标签关系、证据、候选标签、Other 审核和公司 watchlist。
- 新建本项目自己的 SQLite schema，分为来源、RSS 导入批次、文章基础信息、正文、打标状态、打标批次、正式标签、标签证据、候选/Other 审核、公司 watchlist 和词表版本。
- `articles` 表只保存文章基础事实；多值标签不写入 `articles`，统一写入 `article_tags`。
- `tag_evidence` 独立保存每个标签的证据，支持人工复核和重打标追溯。
- `other_candidates` 独立服务活字典维护，候选标签经人工 qualification 后才能进入正式词表。
- `company_watchlist` 独立服务 `company_tags`，支持客户指定公司和历史回填。
- 新增 `v_articles_for_tagging` 作为 OpenClaw 读取待打标文章的稳定视图。

**原因：**

下一步需要将 RSS 原始 JSON 入库，并让 OpenClaw 访问 SQLite 完成打标签。若把标签直接塞进文章表或 JSON blob，后续筛选、重打标、证据审计和活字典维护都会困难。因此本次设计选择规范化存储正式标签与证据，同时保留原始 JSON 和正文，兼顾可追溯性与 Agent 批量处理效率。

**后续修订：**

2026-06-03：008 变更废弃基于 AI 自评分数的候选口径，改为 `suggested_new_tags`、`tag_review_queue`、schema validation、evidence validation 和 post-processing。

---

## 008 | 2026-06-03 | Agent 能力

**类型：** Agent 能力
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户要求，对 `newsletter-tagging/` 进行结构性修改，将 OpenClaw 打标从“Agent 按文字规则理解执行”改为“代码控制流程、LLM 只做语义判断”的批处理 workflow。

核心决策：

- `SKILL.md` 保持精简，只规定必须加载 workflow、prompt、schema、validator 和字典。
- 新增 `product/skills/newsletter-tagging/references/workflow.md`，明确 code responsibilities、LLM responsibilities、状态机、失败分类、needs_review 触发、post-processing、batch/dry-run/retry/logging 和质量指标。
- 新增 `product/skills/newsletter-tagging/references/llm_prompt.md`，作为单篇文章语义判断 prompt 模板。
- `schemas/tag_output.schema.json` 升级为 `newsletter-tagging/v2`，新增 `relevance`、`tagging_decision`、`review_reasons`、`suggested_new_tags`，移除质量自评字段。
- `scripts/validate_tags.py` 升级为 v2 校验器，检查受控词表、证据完整性、公司 watchlist、review reason，并输出 batch metrics。
- `dev/docs/数据库设计.md` 移除正式标签、证据和候选标签中的质量自评字段，新增 `tagging_attempts`、`suggested_new_tags` 和 `tag_review_queue` 设计。
- `README.md` 增加 OpenClaw 打标运行契约、dry run、failed / needs_review 查看、taxonomy 扩展和失败类型说明。

**原因：**

AI 自评分数不透明且不稳定，不适合作为入库或质量控制依据。新的 workflow 用 schema validation、evidence-based tagging、rule-based post-processing、review queue、抽样复核和批次 metrics 替代阈值判断，从而提升批量运行、复核和解释能力。

---

## 009 | 2026-06-03 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户反馈，重构 `dev/docs/标签字段字典.md` 并同步 `newsletter-tagging/`：

- 将标签体系改为两层：基础数据字段标签与 Agent 判断标签。
- 基础数据字段包括来源、抓取方式、抽取状态、语言、地区、来源性质等，优先由代码或轻量规则处理。
- Agent 判断标签压缩为 MECE 维度：`primary_story_type`、`industry_segment`、`product_application`、`strategic_driver`、`value_chain_stage` 和自由实体 `company`。
- 取消公司预设 watchlist。Agent 在文章事件确实与公司有关时自由抽取公司显示名，写入 JSON / 数据库，不回写字典词表。
- `newsletter-tagging` schema 升级为 v3，prompt / workflow / validator 同步改为两层 taxonomy 和公司自由实体抽取。
- 数据库设计将 `company_watchlist` 改为 `company_entities`，用于公司实体归一化、合并和审计，而非限制 Agent 可提取的公司。

**原因：**

旧字典字段过多，部分标签跨层重复，容易让 Agent 过度打标并增加后端客户匹配复杂度。新结构将自动可判定字段和语义判断字段分离，用更少、更独立的维度提升打标稳定性，同时保留公司新闻筛选能力。

---

## 010 | 2026-06-03 | 客户交付

**类型：** 客户交付
**决策方：** User + Claude
**状态：** Confirmed

**内容：**

根据用户要求，调研 `reference/禾大newsletter/` 原型材料（Beauty Monthly Report Prompt、WeChat Article Export、市场监测月报 HTML），为客户禾大 Croda（美妆个护原料商）定制 newsletter，新增两份交付物到 `outputs/禾大Croda/`：

- `禾大Croda-Newsletter需求分析报告.md`：客户画像、监测对象（竞品/代理/客户品牌/媒体五类角色 + 关键词 + 媒体源）、newsletter 内容与 insight 维度、业务需求推测、原型功能分析、与本项目字典的继承/差异、风险与待确认项。
- `标签字段字典-禾大美妆个护版.md`：在父项目两层 MECE 架构上重构的美妆个护原料行业字典，schema 建议 `newsletter-tagging/croda-beauty-v1`。

核心字典决策：

- 完全继承两层模型、MECE、证据驱动、活字典、公司自由实体和证据优先原则。
- Agent 判断词表从食品饮料重构为美妆个护：`industry_segment`、`product_application`、`strategic_driver`、`value_chain_stage` 全部本地化。
- 新增三个客户关键维度：`ingredient_technology`（成分/技术，月报主轴，活字典每两周复核）、`functional_claim`（功效宣称，独立于战略驱动）、`entity_role`（公司角色受控维度，公司名仍自由抽取）。
- 监测公司名单（竞品/客户/媒体 Excel）定位为客户配置 watchlist，不入字典词表，与 `entity_role` 配合做后端匹配。
- 原型中涉及 AI 自评分数的要求按项目决策 008 处理：打标阶段不输出自评字段，质量由证据、规则校验和人工复核控制。

**原因：**

禾大与现有食品饮料字典服务的行业不同，需要行业专属词表才能让打标结果支撑其竞争情报、产品路线图、BD、法规合规、国产替代/出海等业务场景；同时必须沿用本项目已沉淀的字典方法论以保证可维护性与一致性。

**待确认：**

watchlist 同步机制、`ingredient_technology` 活字典节奏、下游品牌新闻相关性边界（见需求分析报告 §7）。

---

## 011 | 2026-06-03 | 数据规范

**类型：** 数据规范
**决策方：** User + Claude
**状态：** Confirmed

**内容：**

根据用户反馈，修订 `outputs/禾大Croda/标签字段字典-禾大美妆个护版.md`（字典版本 v1.1，schema 仍 `newsletter-tagging/croda-beauty-v1`），并同步需求分析报告：

1. **`entity_role` 与 `value_chain_stage` 去重解耦**：经检查二者在"产业链位置"上语义重复（competitor_ingredient≈ingredient_active、customer_brand≈brand_finished_product、distributor_agent≈distribution_channel、cmo_oem≈contract_manufacturing）。将 `entity_role` 收敛为**仅表达"公司相对禾大的业务关系"**：`self / competitor / customer / channel_partner / ecosystem / other`；链位置交回 `value_chain_stage`，"国际/国内"交给 `market_region`，具体公司交给 `company`。给出旧值迁移对照与月报分类还原方式。

2. **`ingredient_technology` 改为内联 `other:` 实时活字典**：字典外成分/技术由 Agent 实时写 `"other:<slug>" + extracted_name + 证据`，文章照常 `tagged`、当下即可检索聚类，不再丢给 `suggested_new_tags` 等人工。晋升改为**频次驱动**（窗口期 ≥2 篇/≥2 来源），人工只看高频列表批量审核、无需重读文章，通过后正式入词表并可重打标历史 `other:`。原则：宁可多打一个 AI 抽取的成分名，也不漏标。封闭枚举字段仍严格受控，不用内联 other。活字典维护改为双路径（A 内联 other / B suggested_new_tags）。

**原因：**

(1) 去重：消除 entity_role 与 value_chain_stage 的重复编码，恢复 MECE，降低后端客户匹配复杂度，且保留"关系"这一 value_chain_stage 无法表达的高价值轴。(2) 实时活字典：人工审核非实时且需重读文章，成本高、会漏掉窗口期内的新成分/技术趋势；改为 AI 实时内联抽取 + 频次晋升，兼顾不漏标与词表纯净。

---

## 012 | 2026-06-03 | 项目管理

**类型：** 项目管理
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户要求，将工作区从上一代食品饮料 newsletter 项目重整为禾大 Croda 定制化情报订阅 Agent：

- 顶层 `README.md`、`dev/docs/项目管理文档.md`、`dev/docs/系统结构.md`、`dev/docs/数据库设计.md`、`dev/docs/里程碑检查清单.md` 改为禾大项目背景。
- `dev/docs/标签字段字典.md` 和 `product/skills/newsletter-tagging/references/标签字段字典.md` 切换为禾大美妆个护版字典。
- `product/skills/newsletter-tagging/SKILL.md`、`references/workflow.md`、`references/llm_prompt.md`、`schemas/tag_output.schema.json`、`scripts/validate_tags.py` 同步为 `newsletter-tagging/croda-beauty-v1`。
- 旧 `app/TradeMediaRSS/`、旧食品饮料标签字典和旧数据流说明归档到 `reference/legacy-food-beverage/`。
- 删除 `dev/temp/pycache`、`reference/wewe-rss/node_modules`、`reference/wewe-rss/.git`、`.DS_Store` 和 `__pycache__` 等非交付残留。
- `app/` 保持为禾大正式应用代码区，新增 `app/README.md` 说明后续结构。
- 修正活跃文档中误写的未来日期 `2026-06-04`，统一为当前日期 `2026-06-03`。

**原因：**

当前客户需求已经从食品饮料行业模板转向禾大 Croda 美妆个护原料行业。若继续保留旧应用代码和旧行业字典在活跃路径，会误导后续开发、打标和项目管理。通过归档旧资产、清理测试残留和同步 Croda 专属 skill，可以保留可复用方法论，同时让当前项目目标、交付物和执行约束保持一致。

---

## 013 | 2026-06-04 | 数据规范

**类型：** 数据规范
**决策方：** User + Claude
**状态：** Confirmed

**内容：**

根据用户对需求分析报告、标签字段字典、数据库设计的反馈，发布禾大字典 v2，并同步 schema、validator、skill 文档、数据库设计与需求报告：

1. **需求报告**：在原型四大深度分析之外新增"重点客户监测"章节，针对 Excel 名单内**全部下游客户品牌**监测卖点、在用成分/技术、质量趋势与对禾大的供应机会；§7 Watchlist 改为已定义方案。
2. **字段 vs 标签严格分离**：来源、语言、地区、来源性质、抓取方式、抽取状态等**字段**由入库脚本在入库前写入文章 JSON，Agent 不重算、不补全；这些字段存文章/批次表，不进 `article_tags`。
3. **主故事类型 `primary_story_type` 改多选**，并合并：`business_transaction`+`capital_finance` → `corporate_move`；删 `safety_quality_issue`（拆入 `regulation_policy`/`corporate_move`）；删 `supply_chain_trade`（拆入 `market_consumer_insight`/`corporate_move`）。
4. **删除 `industry_segment`**（并入 `value_chain_stage`，已解释其定义为"产业环节"而非"受众"，与价值链重复）、**删除 `strategic_driver`**、**删除 `entity_role`**（公司角色改由 Watchlist 匹配得出）。
5. **活字典扩展**：`product_application`、`functional_claim` 的字典外值与 `ingredient_technology` 一样改用内联 `other:<名>` 实时打标（`ingredient_general` 与固定 `other` 合并为活字典 other），频次驱动晋升。
6. **evidence 精简**：每个标签只保留客观证据 `evidence_text`，去掉 `evidence_field` 与 `reason`；`other:` 仍需 `extracted_name`。
7. **数据库**：新增**字典表 `taxonomy_terms`**（picklist 单一事实来源 + 活字典晋升落点，明确 picklist 创建机制）；**`article_tags` 精简为纯 Tag↔文章关联**；`tag_evidence` 去 `evidence_field`/`reason`；删除 `database_query`、`access_status` 及失败枚举、`extraction_status` 仅留 success/partial；`watchlist_entities` 增加规范化别名；新增第六节"数据流与字段归位"，明确哪些字段 RSS→入库、哪些只留抓取日志。
8. **schema/validator/skill/prompt/workflow** 全部同步到 v2 字段集。

**回答用户两个数据库问题：**

- **字典表**：原设计没有，本次新增 `taxonomy_terms` 作为字段可选值 picklist 的单一事实来源；picklist = 该字段 `status='active'` 的词条，种子来自字典词表，活字典晋升回写。
- **字典文章关联表**：即 `article_tags`，打标后生成，本次精简为只存 `article_id + field + label + display_name + status`，溯源/属性不混入。

**原因：**

用户要求把"字段（属性）"与"标签（语义判断）"在字典与数据库层面彻底分清，减少 Agent 过度打标与后端复杂度；删除重复/低价值维度；让新成分/应用/功效能实时被捕获而不堆积到人工审核；并补齐字典表与精简关联表，使数据流可解释、可维护。

**待确认：**

Watchlist 初始名单与同步频率。

---

## 014 | 2026-06-04 | 项目规划

**类型：** 项目规划
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户新的 MVP 构想，确认项目分为两条路线：

1. **MVP 版本（当前主线）**：按月份文件夹处理，不先做数据库。流程为：RSS/raw 抓取记录 → `rss_clean.json` → 小龙虾 1 输出 `tagging.json` → 小龙虾 2 读取近 3 个月最终报告、当月 RSS JSON 和打标 JSON，输出 `report_content.json` → HTML 渲染脚本输出最终 `report.html`。
2. **完整版本（第二版）**：保留 `dev/docs/数据库设计.md` 中的 SQLite、`taxonomy_terms`、`article_tags`、`tag_evidence`、`watchlist_entities`、`report_runs` 等设计，等 MVP 跑通并确认有长期数据管理需求后再接入。

新增 `dev/docs/MVP总体规划.md`，明确：

- 月度目录结构建议为 `outputs/<month>/raw/`、`rss_clean.json`、`tagging.json`、`report_context.json`、`report_content.json`、`report.html`、`run_log.json`。
- MVP 三类工作进度：项目规划与交付物目标、脚本部分、Agent 与控制脚本。
- 小龙虾 1 负责打标 JSON，小龙虾 2 负责报告内容 JSON，HTML 渲染脚本只负责版式。
- MVP JSON 字段需与完整数据库设计保持兼容，未来可通过 adapter 导入数据库。
- 当前不实现 SQLite 入库、数据库状态机、跨月全文检索、长期历史重打标和后台管理界面。

同步更新 `README.md`、`dev/docs/项目管理文档.md`、`dev/docs/系统结构.md`、`dev/docs/里程碑检查清单.md`。

**原因：**

当前客户需求是月度报告，而不是复杂的数据管理平台。以月度 JSON 工件为 MVP 更接近人工工作方式，可降低工程复杂度、提升可读性和调试效率；同时保留数据库第二版设计，避免未来需要长期运营时重做数据结构。

---

## 015 | 2026-06-04 | 项目规划

**类型：** 项目规划
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户反馈，将 MVP 规划从“流程说明”增强为“项目管理执行计划”：

- `dev/docs/MVP总体规划.md` 拆为两部分：第一部分说明 MVP 项目定位、边界、交付目标、角色责任、阶段目标；第二部分针对项目规划、脚本/HTML、Agent/控制脚本三条工作线，定义团队任务、交付物、工作量、验收点和质量门。
- 新增 `dev/docs/MVP关键工件契约.md`，独立保存月度工作包中 raw、`rss_clean.json`、`tagging.json`、`report_context.json`、`report_content.json`、`report.html`、`run_log.json` 的生成者、消费者、字段结构和验收规则。
- 新增 `dev/docs/MVP-Agent开发指引.md`，给后续 coding agent / openClaw 使用，说明渐进式阅读顺序、coding agent、小龙虾 1、小龙虾 2、HTML 渲染脚本和控制脚本的执行边界。
- 同步 `dev/docs/项目管理文档.md` 与 `dev/docs/系统结构.md`，将新增文档纳入正式文档索引。

**原因：**

MVP 后续需要多人或多 Agent 协作，仅描述数据流不足以保证团队对齐。需要从项目经理视角明确每个阶段谁负责、交付什么、需要多少工作量、由谁验收，以及如何把质量问题记录到单一事实源。

---

## 016 | 2026-06-04 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户提供的评估建议，将本次更新前的输出字典完整存档，并发布禾大美妆个护原料标签字段字典 V2：

- `outputs/禾大Croda/Archive/标签字段字典-禾大美妆个护版-V1-存档.md` 保留本次更新前版本。
- `outputs/禾大Croda/标签字段字典-禾大美妆个护版-V2.md` 作为新的客户交付版。
- 同步更新 `dev/docs/标签字段字典.md` 与 `product/skills/newsletter-tagging/references/标签字段字典.md`，保持三份 V2 字典一致。
- 新增 `relevance` 准入标准，明确 `relevant`、`not_relevant`（对应业务口径 irrelevant）与 `unclear`。
- 新增多维协同范式说明，强调 `primary_story_type`、`value_chain_stage`、`ingredient_technology`、`product_application`、`functional_claim`、`company` 组合查询，而非单维度大包大揽。
- `functional_claim` 新增 `anti_glycation`、`blue_light_protection`、`anti_pollution`。
- `ingredient_technology` 新增 `microfluidics`、`stem_cell`。
- 明确 `company` 只写入事件主体、主要合作方或重点分析对象；趋势文章中的顺带案例引用不写入。
- 补充 `cross_chain` 判断示例与 `other:<slug>` 归一化规则。
- 记录不采纳项：不拆分 `corporate_move` 子维度，不新增文章体裁字段。
- 同步 `newsletter-tagging` 的 `SKILL.md`、`llm_prompt.md`、`workflow.md` 与 `validate_tags.py`，确保新增正式标签和边界规则可执行。
- 更新需求分析报告与 README 中的输出字典文件名引用。

**原因：**

V2 字典需要同时服务客户阅读、Agent 打标和后续校验脚本。将准入门槛、维度协同、公司抽取边界和活字典 slug 规则写入正式字典，可减少下游误判；新增亚洲市场高频功效和技术标签，可降低常见趋势被迫落入 `other:` 的频率。

---

## 017 | 2026-06-04 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户对 `source_nature` 与基础字段来源的追问，修订禾大 V2 字典的 `1.1 基础数据字段`：

- 将基础字段表从“处理方式/存放位置”扩展为“RSS/抓取结果是否自带 / 脚本处理方式 / 存放位置 / 备注”。
- 明确 `source_nature` 通常不是 RSS 原始 JSON 自带字段，需由清洗/入库脚本依据 source 配置、域名、公众号来源、发布方与署名规则判断。
- 明确哪些字段主要来自 RSS 原始字段并只需格式规范化，如 `url`、可能存在的 `published_at`、可能存在的 `raw_tags`。
- 明确哪些字段来自 source 配置，如 `source_key`、稳定归一的 `source_name`、默认 `market_region`。
- 明确哪些字段由脚本运行或判断补充，如 `fetched_at`、`ingest_method`、`content_language`、`source_nature`、`extraction_status`、`canonical_url`。
- 同步更新输出 V2 字典、`dev/docs/标签字段字典.md` 与 `product/skills/newsletter-tagging/references/标签字段字典.md`。

**原因：**

后续实现 `rss_clean.json` 清洗脚本时，需要清楚区分“原始 RSS 自带字段”“脚本判断字段”和“格式规范化字段”，避免把来源性质、地区、语言等业务属性误认为 RSS 天然提供。

---

## 018 | 2026-06-04 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户要求，基于客户 Excel 中的“行业媒体”名单与禾大 V2 字典，新增媒体源字段设计与 seed 清单，并按后续反馈收窄字段：

- 新增交付文档 `outputs/禾大Croda/媒体源字段设计与Seed清单.md`。
- 新增可机读 seed `outputs/禾大Croda/source_profiles_seed.json`，包含 Excel 中 13 个行业媒体来源。
- 不再同时保留 `source_channel` 与 `source_nature_default`；合并为一个 `source` 字段。
- `source` 用一个值表达“渠道 + 来源性质”，如 `web_official`、`wechat_official`、`wechat_domestic_media`、`wechat_event_official`、`wechat_third_party_analysis`。
- 保留 `source_name` 作为“具体来源”，保留 `source_list` 作为“Source List”。
- 删除 `source_type` 以及上一版建议新增的扩展字段：`owner_entity_id`、`wechat_account_name`、`wechat_id`、`homepage_url` 等。
- 明确 Excel 中其它公司/品牌/代理商名单优先进入 `watchlist_entities`；只有它们的官网、官方公众号、新闻中心等具体采集入口才进入 `source_profiles`。

**原因：**

客户需要区分“网页抓取的巴斯夫官网”“巴斯夫微信公众号”“微信公众号里的行业媒体美妆小报”等来源，但无需保留过多拆分字段。使用单一 `source` 合并表达渠道与来源性质，可降低配置成本，同时保留必要的业务区分。

---

## 019 | 2026-06-04 | 数据规范

**类型：** 数据规范
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户对更新后 media list 与 MVP 数据流的反馈，重新修订禾大 V2 字典中的基础数据字段：

- 不再把 `source_nature` 作为独立文章基础字段；其业务含义并入媒体源配置的 `source` 字段。
- `source_name/source_key` 命中 `source_profiles` 后，清洗脚本自动带出 `source`、`source_list`、`ingest_method`、默认 `market_region` 和默认 `language/content_language`。
- 新增 `article_id` 作为 MVP 主串联键，用于连接 `rss_clean.json.articles[]`、`tagging.json.items[]` 和 `report_content.json` 中的报告条目。
- 新增轻量 `raw_ref`，用于从 `rss_clean.json` 回溯原始抓取文件和条目位置，不作为 Agent 标签判断依据。
- 补齐 `title`、`summary`、`content`、`content_length`、`extraction_status` 等打标与 QA 所需基础字段。
- 同步更新 `dev/docs/MVP关键工件契约.md` 的 `rss_clean.json` 示例与约束。
- 同步更新 `dev/docs/系统结构.md`、`newsletter-tagging` prompt/workflow/schema/validator 的基础字段说明。
- 同步更新 `outputs/禾大Croda/媒体源字段设计与Seed清单.md` 中“与文章 JSON 的关系”示例。

**原因：**

MVP 的关键不是让 Agent 猜来源性质，而是让清洗脚本基于 media list/source profile 生成稳定基础字段。`article_id` 负责跨 JSON 工件追溯，`source_key/source_name` 负责来源配置匹配，`source` 负责表达渠道与来源性质，从而保持流程轻量但可串联、可复核。

---

## 020 | 2026-06-05 | HTML 配置测试

**类型：** 测试工件
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户提供的 3 个 HTML 链接导出文件和禾大 newsletter MVP 需求，新增开发/测试用 HTML 配置 fixture：

- 将 3 个链接导出 JSON 归档到 `reference/禾大newsletter/link_exports/`。
- 在 `outputs/禾大Croda/html-config-fixtures/` 新增 3 份 `report_content` 测试配置：
  - `report_content_2605_workbuddy_cn.fixture.json`
  - `report_content_2605_bilingual_nav.fixture.json`
  - `report_content_2605_en.fixture.json`
- 新增 `outputs/禾大Croda/html-config-fixtures/README.md`，记录最终 HTML 交付物判断：按月静态 HTML 由 `report_content.json` 驱动，渲染器只处理版式、导航、卡片、图表和链接；业务判断来自上游 JSON 与 Agent。
- 3 份 fixture 分别覆盖中文 workbuddy、双语带锚点导航、英文报告三类测试场景，并保留外链去重、重复 URL、长标题、附录和来源域名图表测试数据。

**原因：**

HTML 渲染器开发前需要可直接使用的配置样例，既能体现客户原型中的最终交付物需求，也能让后续开发在没有完整 Agent 2 输出时先验证版式、导航和链接处理。

---

## 021 | 2026-06-05 | 客户方案口径与 WorkBuddy 使用体验

**类型：** 产品需求 / 客户沟通口径
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户在 `Croda RSS project proposal_260605.html` 中对方案 PPT 的重写，确认客户方案和 MVP 需求应按以下口径理解：

- 方案主线从“系统化月度情报工作流”进一步收敛为“把 WorkBuddy 的一键草稿升级为可信任、可编辑的月度情报工作流”。
- 客户真正需要解决的不是“能生成页面”，而是“敢用报告”：来源稳定、分类稳定、出处可追溯、内容可局部修改、报告结构每月一致。
- prototype / demo 的问题需要具体化为使用风险，而不是笼统说“工程不完整”：信源看运气、公众号覆盖不足、抓取内容脏、分类随机、数字和观点缺少出处、每月结构不一致、没有近月记忆、无法局部编辑。
- 月度工作流需要以用户视角表达：用户在 WorkBuddy 发起任务，系统后台抓取、清洗、打标、生成结构化草稿；用户查看初稿、通过聊天局部修改、确认终版并导出 HTML。
- 打标阶段默认系统自动完成，不要求客户逐条介入；客户主要出现在看报告、改报告、确认关键业务判断和后续调整标签体系时。
- 交付物新增“可编辑结构化草稿”作为客户可感知能力，用于支持在 WorkBuddy 中移动、删除、改写单条报告内容，局部修改不影响全局。
- 公众号/微信生态应作为稳定来源接入的重要组成部分，与 RSS 和 AI 搜索补充共同构成来源能力。
- 本轮客户确认项从三类扩展为四类：需求场景对齐、标签内容与报告标准、工作流贴合度、人工微调流程。

**原因：**

此前方案更偏“技术能力与项目工作量说明”，客户可能仍难以判断为什么 demo 不能直接用于月度工作。新稿把风险和价值转成客户能感知的语言：从“这个东西能不能出报告”转向“我能不能放心每月用、能不能追溯、能不能改、改了会不会影响整份报告”。这要求后续开发文档和验收标准同步关注 WorkBuddy 交互式编辑、结构化中间件、证据追溯和微信生态覆盖。

---

## 022 | 2026-06-10 | 项目结构

**类型：** 项目结构
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户提供的 `croda-template`，当前 Croda 项目改用新的 Croda Project Management Agent 骨架：

- 根级入口改为模板版 `AGENTS.md`，要求后续先读 `project-management/skills/active/croda-project-management/SKILL.md`。
- 新增 `product/`：承载 React 前端、Python 后端、spider、parameter-file 和 runtime output。
- 新增 `project-management/`：承载内部项目管理 skills、客户资料、参考系统、归档内容、共享 docs、work log 和临时材料。
- 旧 `newsletter-tagging/` 迁入 `product/skills/newsletter-tagging/`，并同步运行输出路径到 `product/output/`。
- 旧 `dev/docs/` 迁入 `project-management/dev/docs/`；旧 work log 改为 `project-management/dev/notes/work-log/codex/YYYYMMDD.md`。
- 旧 `reference/` 资料分别迁入 `project-management/reference/source-materials/`、`project-management/reference/examples/` 和 `project-management/reference/archive/`。
- 旧 `outputs/禾大Croda/` 中的配置类文件迁入 `product/parameter-file/`，报告/demo/fixture 迁入 `product/output/`。
- 新增 `.github/scripts/validate_structure.py` 和 `.github/workflows/validate-structure.yml` 作为结构校验。

**原因：**

Croda 项目后续需要和新 Project Management Agent 保持一致，避免继续使用旧根级 `app/`、`outputs/`、`dev/`、`reference/` 和 `newsletter-tagging/` 造成路径混乱。新结构把产品代码、运行产物、项目管理、参考资料、产品流程 skills 和内部管理 skills 分层，便于后续多人或多 Agent 协作。

---

## 023 | 2026-06-10 | 项目结构

**类型：** 项目结构
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

根据用户澄清，`newsletter-tagging` 是客户交付产品流程中的小龙虾/OpenClaw skill，不是内部项目管理 skill：

- 将 `newsletter-tagging` 从 `project-management/skills/active/newsletter-tagging/` 移到 `product/skills/newsletter-tagging/`。
- 更新 `AGENTS.md`，新增 Skill Placement Rule：客户交付、客户安装、产品流程读取的 skills 放 `product/skills/`；内部项目管理、仓库组织、协作治理 skills 才放 `project-management/skills/`。
- 更新 `project-management/skills/active/croda-project-management/SKILL.md` 与 `references/directory-and-output-rules.md`，明确上述区分。
- 更新 `.github/scripts/validate_structure.py`，要求 `product/skills/newsletter-tagging/` 存在，并禁止 `project-management/skills/active/newsletter-tagging/` 回流。
- 更新系统结构、目录整理说明和 MVP Agent 开发指引中的路径。

**原因：**

此前规则只写了 `project-management/skills/` 用于 “Agent skills / reusable skill deliverables”，没有区分“内部项目管理 skill”和“客户产品流程 skill”。这会误导后续 Agent 把小龙虾打标 skill 放到项目管理区。新规则以交付对象和运行位置区分：客户会安装或产品流程会读取的 skill 属于产品交付物，应放在 `product/skills/`。

---

## 024 | 2026-06-13 | HTML 月报 demo 改造 PRD

**类型：** 产品需求 / 前端 demo / 配置渲染
**决策方：** User
**状态：** Draft

**内容：**

根据用户最新反馈，先更新 PRD 和设计需求，待用户确认后再开发 HTML demo：

- 新增 `project-management/dev/docs/HTML月报Demo改造PRD.md`，作为下一版客户可见 HTML demo 的设计需求草稿。
- 顶部数据卡片调整为 5 个固定 demo KPI：
  - `169`：2025年新原料备案数量（+88%）
  - `89.3%`：国产新原料占比
  - `~$35B`：全球化妆品原料市场规模
  - `8.2%`：中国市场年复合增速
  - `30+`：本月监测竞品动态
- 正文栏目调整为 6 个固定栏目：
  1. 市场快讯·本月Top10热点事件
  2. 竞品动态监测
  3. 新成分 & 新趋势情报
  4. KA 监测
  5. 市场活动汇总
  6. 法规政策
- 底部保留“数据来源与链接汇总”作为附录，不参与 `primary_section` 栏目竞争。
- 明确 HTML demo 应使用固定版式、配置驱动渲染：正式内容从 `product/output/result/report/<YYYYMM>/report_content.json` 进入模板，模板只负责版式、交互、品牌视觉和链接渲染。
- 明确标签对报告写作的作用：栏目是一等判断；轻量标签用于搜索、筛选、趋势聚合、图表、术语 definition 和证据追溯，而不是只作为视觉标签展示。
- 新增“一键切换英文”需求：中英文文本来自配置，不在浏览器端实时机器翻译。
- 新增术语 definition 需求：桌面端 hover/focus、移动端点击展示解释浮层，解释内容支持中英文。
- 明确视觉优先级：优先参考 Croda 客户官网品牌绿色调，其次参考旧 WorkBuddy prototype 的阅读习惯，再复用 kai-report-creator 的报告 shell 能力。
- 本轮不生成或修改 HTML，先等待用户确认 PRD。

**原因：**

上一版 demo 已能表达标签驱动和来源追溯，但客户反馈要求优先回到 Croda 品牌与新的栏目体系。与此同时，用户明确指出“打标签对于最后写报告的作用”不能被弱化，因此新版 PRD 把标签从页面装饰提升为报告分析、筛选、聚合、definition 和证据追溯的基础能力。先冻结 PRD 可避免 HTML 开发继续沿用旧栏目、旧 KPI 或旧路径。

---

## 025 | 2026-06-13 | HTML 月报 demo PRD 待确认事项答复

**类型：** 产品需求 / 前端 demo / 客户确认口径
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户对 PRD 待确认事项的答复，更新 `project-management/dev/docs/HTML月报Demo改造PRD.md`：

- 顶部 5 个 KPI 长期固定为 demo 指标；正式版第一阶段不做月度更新。
- 英文版需要完整英文正文，不只翻译栏目标题、KPI、摘要和 tooltip。
- Definition 术语范围覆盖标签、成分、栏目和法规术语。
- “市场快讯·本月热点事件”最多 10 条，不要求固定满 10 条；当月不足 10 条时有多少放多少。
- KA 监测名单直接沿用 V4 Watchlist，第一阶段不要求客户另行提供 priority KA 子名单。
- 正式 HTML demo 保留 WorkBuddy 局部修改示例。
- 用户在 PRD 中补充新闻卡片详情交互：点击卡片后先打开放大详情窗口，展示标签、文章标题、一段话新闻解释、媒体源和原文链接，点击链接跳转到原文。
- 对客户侧表达进行收敛：客户演示重点是报告结构稳定、内容可追溯、每月版式一致；`report_content.json` 和配置渲染作为内部实现口径，不作为客户演示时的主要表达。

**原因：**

这些答复解决了上一版 PRD 中最影响 HTML demo 实现范围的开放问题：KPI 是否动态、英文覆盖深度、definition 覆盖范围、市场快讯条数、KA watchlist 来源以及 WorkBuddy 编辑示例是否保留。PRD 现在可以作为进入 fixture 和 HTML demo 实现前的更稳定设计依据，但仍需用户确认整体文档后再开始 HTML。

---

## 026 | 2026-06-13 | HTML 月报 demo fixture 与静态页面

**类型：** 前端 demo / 配置渲染 / 客户确认物
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

用户确认整体 PRD 后，进入 fixture 和 HTML demo 实现，新增以下输出：

- `product/output/result/report/202606/html-demo/report_content.json`
- `product/output/result/report/202606/html-demo/report.html`
- `product/output/result/report/202606/html-demo/README.md`

实现范围：

- 5 个顶部 KPI 使用长期固定 demo 指标，正式第一阶段不做月度更新。
- 正文固定为 6 个栏目：市场快讯·本月热点事件、竞品动态监测、新成分 & 新趋势情报、KA 监测、市场活动汇总、法规政策。
- 底部保留数据来源与链接汇总。
- HTML demo 使用 Croda 官网优先的绿色品牌方向。
- 页面支持完整中英文切换。
- 术语 definition 覆盖标签、成分、栏目和法规术语。
- 新闻卡片支持详情弹窗，并从 `source_ref_ids` 回连来源链接。
- 搜索支持公司、成分、功效、栏目、来源和标签。
- KA 监测沿用 V4 Watchlist 口径。
- 保留 WorkBuddy 局部修改示例。
- HTML 包含 kai-report-creator 标准 shell 所需 ID 与导出入口，用于客户确认前端展示。

**原因：**

客户现在需要确认的是 HTML 前端最终会如何呈现，而不是先完成真实抓取、真实标签或正式报告内容。该 demo 用 dummy data 演示固定版式、配置驱动、中英文、definition、来源追溯和局部修改体验，为后续正式 renderer 和 `report_content.json` schema 冻结提供可视化基准。

---

## 027 | 2026-06-13 | 客户可见版 HTML demo 交互与文案收敛

**类型：** 前端 demo / 客户反馈 / 配置渲染
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户对客户演示页的反馈，更新 `product/output/result/report/202606/html-demo/report_content.json` 与 `report.html`：

- 搜索区域新增标签下拉筛选，标签来源于 `glossary_term_ids` 与非栏目类 `tags`。
- 移除页面中面向内部说明的 demo 文案、客户确认说明、内部实现口径和 WorkBuddy 局部修改示例。
- 移除每个栏目右上角的栏目 ID / max badge。
- 移除与栏目重复的可见标签，例如 `ka_watch`、`market_flash` 等；保留有分析意义的成分、趋势、法规和功效标签。
- KA 监测卡片强化公司名视觉层级，便于客户直接扫到关键客户。
- 六个正文栏目和底部数据来源附录均支持折叠/展开。
- 删除解释性栏目说明文案，使页面更像客户最终看到的月报 HTML，而不是实现说明页。
- 继续保留 kai-report-creator 标准 shell、摘要卡、导出入口、编辑热区、TOC、完整中英文、详情弹窗、definition tooltip 和来源链接。

**原因：**

客户侧需要确认的是最终报告页面长什么样，而不是阅读“这是 demo / 这是内部实现”的说明。标签在页面上应服务筛选、术语解释和分析聚合，不能与栏目名重复造成噪音。WorkBuddy 局部编辑能力保留为 HTML shell 的隐藏编辑热区，不再以单独示例卡片占用客户报告正文。

---

## 028 | 2026-06-13 | HTML demo 截图反馈版式修复

**类型：** 前端 demo / 页面排版
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户截图反馈，修复 `product/output/result/report/202606/html-demo/report.html` 的版式问题：

- 修复折叠栏目标题被渲染成浏览器默认大按钮的问题。
- 为 `section-toggle`、`section-body`、`card-grid`、`source-card`、`toolbar-left`、`company-name` 等补齐客户可见版布局样式。
- 统一动态生成的 `card-grid` 与 CSS 中旧 `cards-grid` 命名，避免卡片退化为纵向大文本流。
- 数据来源附录恢复为双列卡片布局，而不是纯文本列表。
- 竞品动态与 KA 公司名保持突出，但不再压迫内容宽度。

**原因：**

截图显示折叠按钮、市场快讯列表、竞品矩阵和来源附录出现明显排版退化。根因是 HTML 生成逻辑使用了新的 class 名，但旧 CSS 中部分选择器未覆盖，导致浏览器默认样式接管。此次通过覆盖样式修正客户演示页面的视觉稳定性。

---

## 029 | 2026-06-13 | V4 标签层级、色彩与公司名双语显示

**类型：** 前端 demo / 标签体系 / 客户反馈
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户截图反馈和 V4 字典，更新 `report_content.json` 与 `report.html`：

- 标签筛选改为两级：一级为 V4 标签大类，二级为该大类下的具体标签字段。
- V4 大类包括主故事类型、原料与技术、功效宣称、产品/应用场景、法规与合规。
- 二级词表参考 `标签字段字典-禾大美妆个护版-V4-draft.md`，例如“原料与技术”下包含透明质酸/玻尿酸、多肽、PDRN、包封/递送技术、绿色化学/可持续工艺等。
- 新闻分类（如“竞品发布”）从普通 meta 文本改为高亮 chip，并按类型使用不同颜色。
- 标签 chip 按 V4 大类使用不同颜色，不再全部使用同一种绿色。
- 各报告板块增加不同色彩强调线，帮助客户区分栏目。
- 公司名新增 `company_display.zh/en`，中文页面显示中文公司名，英文页面显示英文公司名。

**原因：**

客户原型中的标签具有颜色和分类层级，标签不是并列关键词列表。此次把标签展示对齐 V4 字典结构，让筛选、颜色和正文可读性同时服务客户理解。

---

## 030 | 2026-06-13 | 栏目标签与字典字段标签边界确认

**类型：** 前端 demo / 标签体系 / 客户反馈
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户最新截图反馈，继续调整 `product/output/result/report/202606/html-demo/report.html`：

- 日期旁边的高亮 chip 只显示该条内容所属栏目，例如“市场快讯”“竞品动态监测”等。
- 正文底部的小标签只显示 V4 字典里的具体字段，不再显示新闻分类、栏目重复标签或其他临时字段。
- 标签筛选收敛为单个 dropdown：初始展示“全部类型”和标签大类；选择某个大类后，在同一个 dropdown 内展示该大类下的具体标签字段。
- 删除旧新闻分类 chip 样式，避免页面或代码层面继续混淆“栏目”和“标签字段”。

**原因：**

客户需要看到的是最终报告 HTML 的信息层级：栏目用于说明内容归属，字典字段用于筛选、解释和分析聚合。新闻分类或临时字段如果与字典标签并列，会削弱标签体系的业务含义，也会让筛选交互显得不稳定。

---

## 031 | 2026-06-14 | 标签筛选改为 Hover 右展级联菜单

**类型：** 前端 demo / 标签筛选交互 / 客户反馈
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户截图反馈，调整 `product/output/result/report/202606/html-demo/report.html`：

- 将标签筛选从原生 `<select>` 改为自定义级联 dropdown。
- 左侧菜单固定展示“全部类型”和 V4 标签大类。
- 鼠标 hover 或键盘 focus 到某个标签大类时，右侧展开该分类下的具体标签字段。
- 点击“全部类型”重置所有标签筛选；点击大类筛选该大类全部标签；点击右侧具体字段筛选该字段。
- 保持现有 V4 字典字段、栏目 chip、正文底部标签和筛选逻辑不变，仅调整前端交互形态。

**原因：**

客户希望标签筛选更接近桌面级联菜单，而不是选中大类后替换同一个下拉列表。Hover 右展能同时展示标签分类和具体字段之间的从属关系，更符合 V4 字典的层级结构。

---

## 032 | 2026-06-16 | V4 正式栏目收敛与热点新闻边界确认

**类型：** 数据规范 / 栏目体系 / 报告编排
**决策方：** User
**状态：** Confirmed

**内容：**

根据用户最新反馈，更新 V4 字典及小龙虾 1 相关草稿约束：

- 正式栏目标签只保留 5 类：`competitor_watch` 竞品动态监测、`ingredient_innovation` 新成分&新趋势情报、`ka_watch` KA 监测、`market_event` 市场活动汇总、`regulation_policy` 法规新政策。
- “行业新闻快讯” / “热点新闻”不作为打标标签，也不进入 `section.primary_section` 或 `section.secondary_sections`。
- 热点新闻由报告 Agent 在内容编排阶段，从 5 个正式栏目中挑选最多 10 条最相关内容形成展示板块。
- 删除小龙虾 1 输出中的 `is_market_brief_candidate` / `is_customer_watch_candidate` 标志位口径；KA 作为正式栏目由 `ka_watch` 表达。
- 同步更新 V4 字典、V3/V4 schema 草稿、LLM prompt 草稿、validator 草稿与 HTML 月报 Demo PRD 中的栏目边界说明。

**原因：**

“热点新闻”是报告阅读体验中的编辑编排层，不是文章本身的语义标签。如果在打标阶段单独输出热点标签，会造成栏目体系混乱，也会让模型误以为需要先判断“是否热点”。正确流程是先完成 5 个正式栏目归属，再由报告 Agent 按相关性和重要性挑选热点内容。

---

## 033 | 2026-06-18 | script-tagger 模块 B 与配置 seed

**类型：** 数据处理 / 脚本打标 / MVP 工程化
**决策方：** 本地变更补记
**状态：** Confirmed

**内容：**

根据 2026-06-18 本地工作区盘点，6/16 后已新增一条配置驱动的脚本打标层：

- 新增 `product/script-tagger/script_tagger.py`，作为模块 B：在已抽好正文的文章包上补充脚本字段。
- 新增 `product/script-tagger/validate_article_package.py`，校验 `待打标.json` 的基础字段、`spans`、公司、成分、活动字段和自洽关系。
- 新增配置 seed：
  - `product/parameter-file/watchlist/watchlist_entities_seed.json`
  - `product/parameter-file/watchlist/validate_watchlist.py`
  - `product/parameter-file/ingredient-alias/ingredient_alias_seed.json`
  - `product/parameter-file/event-keywords/event_keywords_seed.json`
- 新增运行产物 `product/output/result/tagging/待打标.json` 与 `run_log.json`，当前样本共 450 条，通过本地 validator。
- 新增 `project-management/dev/docs/mvp/脚本层-清洗入库与脚本打标-开发需求与设计.md`，明确 cleaner 与 script-tagger 分层：脚本层只做 factual extraction，不做 relevance / section 语义判断。

**边界：**

- 当前脚本输入是 `project-management/reference/examples/all-articles(1).json`，不等同于正式 spider 输出。
- 当前脚本明确跳过 source profile 派生与真实 HTML 清洗，`published_at` 对不可靠输入置空。
- 当前输出路径是 `product/output/result/tagging/待打标.json`，与 `AGENTS.md` 中 `product/output/待打标.json` 的固定交接路径存在待对齐项，已进入问题跟踪。

**原因：**

栏目打标测试显示公司、成分、活动等事实性字段适合脚本先抽取，再交给小龙虾 1 进行语义判断。将词表外置为配置 seed，可以避免业务词表写死在代码里，也便于后续运营维护。

---

## 034 | 2026-06-18 | 栏目打标实验材料与测试结果归位

**类型：** 测试策略 / 栏目打标 / 标签体系
**决策方：** 本地变更补记
**状态：** Confirmed

**内容：**

本地新增栏目打标实验文档和产物，现归位如下：

- 测试方案与报告位于 `project-management/dev/docs/section-tagging-test/`。
- 测试 fixtures、gold standard、runs、analysis 位于 `product/output/test-results/section-tagging-test/`。
- 原始大 JSON 样本保留在 `project-management/reference/examples/all-articles(1).json`，不复制进测试文档区。
- V3/V4 草稿 schema、prompt 和 validator 位于 `product/skills/newsletter-tagging/`。

实验结论已经反映到 032：正式打标栏目收敛为 5 类，热点新闻由报告 Agent 编排，不作为小龙虾 1 输出字段。

**原因：**

栏目打标实验的价值不只是一次测试输出，而是为标签体系、脚本字段下放、Agent 输出 schema 和报告编排边界提供依据。测试材料必须与原始参考资料、运行产物和正式产品 skill 分开保存，避免后续 agent 读取时混淆。

---

## 035 | 2026-06-18 | HTML demo 评审材料归档与运行期 report 骨架恢复

**类型：** 前端 demo / 目录整理 / 项目管理
**决策方：** 本地变更补记 + Codex
**状态：** Confirmed

**内容：**

根据本地文件盘点，HTML demo 与旧 proposal 输出被重新归位：

- 客户评审用 HTML demo 迁入 `project-management/dev/docs/report/202606/html-demo/`。
- 该 demo 现在由 `render_report.py` 读取 `report_content.json`，生成：
  - `report-internal.html`：内部调整版，含建议重写和一键提交给报告 Agent 的模拟控件。
  - `report-final.html`：最终展示版，不含内部调整控件。
- 旧 proposal / demo 输出迁入 `project-management/dev/docs/report/` 或 `project-management/reference/archive/proposal-deck/`。
- `product/output/result/report/` 恢复为运行期标准输出目录，并通过 `.gitkeep` 保留空骨架，避免结构校验在 fresh checkout 中失败。

**原因：**

当前 HTML demo 是客户确认和团队评审材料，不是正式月度运行输出。将评审材料放在 `project-management/dev/docs/report/`，同时保留 `product/output/result/report/` 作为未来正式运行输出目录，可以减少“文档 demo”和“生产输出”的混淆。

---

## 036 | 2026-06-18 | 字典外新词补充机制 PRD

**类型：** 产品需求 / 标签体系 / MVP 工作流
**决策方：** User + Codex
**状态：** Confirmed

**内容：**

新增 `project-management/dev/docs/mvp/字典外新词补充机制PRD.md`，明确 V4 脚本打标与栏目打标 Agent 的新词补充边界：

- script-tagger 继续负责固定词表标签，不做字典外新词发现。
- 栏目打标 Agent 在判断 `relevance` 和 `section` 时，可顺手补充最多 1 个文章主角级 `other:` 新词。
- `other_terms` 只允许覆盖 `ingredient_technology` 和 `functional_claim`。
- `product_application` 不做 `other:`；未命中固定词表则为空。
- Agent 不复核脚本已经命中的固定标签，不修改脚本标签，不抽取成分表或长列表里的全部未知词。
- 报告展示 `display_name`，不展示“其他”；内部保留 `other:<slug>` 供后续人工整理或二期活字典晋升。

**原因：**

固定脚本词表能保证稳定性，但无法发现尚未进入字典的新成分、新技术或新功效。把字典外新词补充放到栏目打标 Agent 这一趟中，可以利用 Agent 对文章主线的理解，同时避免新增独立审核 Agent 或让 AI 接管脚本标签。每篇最多 1 个 primary `other:` 的限制可避免长列表噪音和标签膨胀。

---

## 037 | 2026-06-18 | V4 脚本配置拆分与 `primary_story_type` 弃用

**类型：** 数据规范 / 脚本配置 / 标签体系
**决策方：** 本地变更补记
**状态：** Confirmed

**内容：**

根据本地文件盘点，V4 脚本层配置已进一步拆分：

- `product/parameter-file/ingredient-alias/ingredient_alias_seed.json`：28 个 ingredient / technology entries，353 个 aliases / keywords。
- `product/parameter-file/functional-claim/functional_claim_seed.json`：16 个 functional claim entries，167 个 aliases / keywords。
- `product/parameter-file/product-application/product_application_seed.json`：8 个 product application entries，126 个 aliases / keywords。
- `product/parameter-file/event-keywords/event_keywords_seed.json`：6 个 event type entries，57 个 aliases / keywords。
- `product/parameter-file/watchlist/watchlist_entities_seed.json`：73 个实体，183 个去重别名，9 个英文别名待运营补全。
- `product/parameter-file/archive/story-type/story_type_seed.json`：`primary_story_type` 原脚本配置归档。
- `product/parameter-file/tag-dictionary/archive/deprecated-primary_story_type-v4.md`：记录 `primary_story_type` 弃用原因。

V4 现行口径：

- `primary_story_type` 不再作为脚本派生标签。
- 小龙虾 1 的 AI 输出以 `section.sections` 平级多标签为核心，不做 primary / secondary 排序。
- `exclude` / `needs_review` 是互斥系统状态，不能与正式栏目混用。
- 描述性标签由脚本配置产生，不进入 AI 输出。

**原因：**

第三轮探针显示 `primary_story_type` 作为纯关键词脚本标签区分度过低，容易变成“几乎每篇都命中多个类型”。它的语义价值已被 AI 的栏目判断覆盖，因此归档保留、停止作为脚本字段输出。其他事实性字段继续拆成独立 seed，保持配置驱动和可运营维护。

---

## 038 | 2026-06-18 | HTML demo 内部审稿 apply_review 流程

**类型：** 前端 demo / 内部审稿 / 报告 Agent 协作
**决策方：** 本地变更补记
**状态：** Confirmed

**内容：**

202606 HTML demo 新增内部审稿应用脚本：

- `project-management/dev/docs/report/202606/html-demo/apply_review.py`
- internal HTML 可导出 `croda-report-review/v1` 结构的 review file。
- `apply_review.py` 对 `delete` 和部分 `replace` 做确定性处理：
  - `delete` item：直接删除对应卡片。
  - `replace` KPI value：直接替换 KPI 数值。
  - `replace` item summary：可替换指定语言 summary；如要求双语，另一语言进入 AI translation pending。
- `instruct` 或无法确定性映射的结构化修改不直接写入，而是列入 routed-to-AI Agent 清单。
- 脚本不覆盖原 `report_content.json`，默认输出 `<report>.reviewed.json` 或 `-o` 指定路径。

**原因：**

内部审稿需要区分“可确定性执行的局部编辑”和“需要报告 Agent 语义改写的修改意见”。先把 delete / replace 这类确定性操作脚本化，可以降低报告 Agent 负担，也能让内部 review flow 更接近未来 WorkBuddy 式局部修改契约。

---

## 039 | 2026-06-20 | HTML 审阅版“其他-X 待确认”与人工补充标签

**类型：** 产品需求 / 前端审阅版 / 标签体系
**决策方：** 本地变更补记
**状态：** Confirmed

**内容：**

新增 `project-management/dev/docs/mvp/前端PRD-HTML审阅版-标签与其他确认.md`，并在 202606 HTML demo 中落地以下审阅版能力：

- 后端/数据口径改为：AI 只判断 `relevance` 与平级多选 `section.sections`；脚本判断固定标签；后处理根据“栏目 + 脚本标签缺口”补 `其他-X 待确认`。
- `其他-X` 是通用占位，不是具体成分名，也不是正式字典标签。
- 审阅版前端新增 `pending_review` 标签组：
  - `other_ingredient_tech` / 其他：成分/技术
  - `other_claim` / 其他：宣称
  - `other_event` / 其他：活动
  - `other_regulation` / 其他：法规
- 正文卡片不显示裸 `Other`；待确认项以“🔍 其他：X”样式出现，并与正式标签视觉区分。
- 审阅版支持人工补充标签，人工动作只写入 review record，不回写正式字典或配置文件。
- 人工处理 `其他-X` 支持保留、删除、注明具体内容、建议入字典。
- 原 `project-management/dev/docs/mvp/字典外新词补充机制PRD.md` 的 `other_terms` 方案已标记为 historical draft，不再作为实现依据。

**原因：**

相比让 Agent 直接输出具体 `other_terms`，`其他-X 待确认` 更可控：它只提示“这里可能有字典外内容需要人看”，不把未知词直接当成系统识别结果，也不污染正式字典。前端审阅版负责把这些待确认项集中筛出，并沉淀人工判断，为后续活字典人工晋升提供依据。

---

## 040 | 2026-06-20 | HTML demo JSON 恢复工具 `extract_json.py`

**类型：** 前端 demo / 工具 / 可追溯性
**决策方：** 本地变更补记
**状态：** Confirmed

**内容：**

新增 `project-management/dev/docs/report/202606/html-demo/extract_json.py`：

- 从渲染后的 `report-internal.html` 或 `report-final.html` 中读取内嵌的 `<script id="report-data" type="application/json">`。
- 无 DOM scraping，直接恢复 renderer 内嵌的完整 JSON config。
- 默认输出 `report_content.extracted.json`，也支持 `-o` 指定路径。
- 已通过烟测从 `report-final.html` 恢复 JSON，统计为 sections=6、kpis=3、kpi_fixed=4、glossary=51、sources=10、tag_categories=6。

**原因：**

HTML demo 现在是自包含文件，可能在客户或团队审阅环节离线传播。保留 JSON 恢复工具，可以在只拿到 HTML 的情况下恢复 `report_content.json`，便于追溯、复现、审稿和后续再渲染。
