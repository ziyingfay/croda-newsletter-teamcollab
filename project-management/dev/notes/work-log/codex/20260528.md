# Work Log 2026-05-28

## 本次目标

按用户指定的 Codex 项目模板，将当前目录中的 TradeMediaRSS 项目整理成可持续维护的项目工作区。

追加目标：基于当前项目进展，重新回顾现有字段和标签，建立字典式标签体系。

## 已完成

- 执行 `bootstrap_project.sh MyNewProject`，生成模板目录。
- 将 `TradeMediaRSS/` 迁入 `MyNewProject/app/TradeMediaRSS/`。
- 将 `wewe-rss/` 迁入 `MyNewProject/reference/wewe-rss/`，作为参考系统保留。
- 将 `__MACOSX/` 迁入 `MyNewProject/dev/temp/imported-archive-metadata/`。
- 更新工作区 `README.md`、项目管理文档、需求变更日志、系统结构和测试记录。
- 新增工作区 `.gitignore`，避免虚拟环境、依赖目录、缓存和临时输出污染版本控制。
- 基于现有媒体配置、RSS JSON、正文样本、CSV/Markdown 媒体清单和参考系统 Prisma schema，新增 `dev/docs/标签字段字典.md`。
- 建立字段字典、标签命名空间、受控标签词表、中文原始标签归一化规则和后续打标规则。
- 发现 `config.py` 中 `funzionali_it` 与 `ingredienti_alimentari_it` key 重复导致媒体数量口径不一致，已记录为 I-001。

## 验证

- 已记录工作区初始化检查。
- 已对核心 Python 文件执行 `py_compile` 并通过。
- macOS Python 默认缓存路径在沙箱中不可写，因此使用 `PYTHONPYCACHEPREFIX=dev/temp/pycache` 将缓存重定向到工作区临时目录。
- 已执行字段/标签体系盘点：确认实际生效媒体源 49 家，`WORKING` 42 家；抽样确认 RSS 输出字段、正文样本字段、CSV 媒体清单字段和参考系统存储字段。

## 后续建议

- 安装 `app/TradeMediaRSS/requirements.txt` 后运行 `python run_fetch.py` 做完整功能验证。
- 明确下一阶段是继续 Python 脚本路线，还是吸收 `wewe-rss` 的服务端/前端架构。
- 下一步可将 `标签字段字典.md` 落到代码：补齐 `source_key`、`fetched_at` 输出，新增媒体源标签配置和文章自动打标模块。
