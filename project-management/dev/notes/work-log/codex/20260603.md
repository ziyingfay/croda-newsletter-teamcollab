# 工作日志 - 2026-06-03

**日期：** 2026-06-03  
**工作时长：** 本次会话  
**工作阶段：** OpenClaw 打标 workflow 结构化改造

---

## 今日完成

1. 重构 OpenClaw `newsletter-tagging` skill
   - 状态：Done
   - 备注：`SKILL.md` 改为轻量入口，明确代码控制 workflow，LLM 只做语义判断。

2. 新增结构化 workflow 和 LLM prompt
   - 状态：Done
   - 备注：新增 `newsletter-tagging/references/workflow.md` 和 `newsletter-tagging/references/llm_prompt.md`。

3. 升级打标输出 schema 和校验脚本
   - 状态：Done
   - 备注：`schema_version` 升级为 `newsletter-tagging/v2`，移除 confidence，新增 relevance、tagging_decision、review_reasons、suggested_new_tags；`validate_tags.py` 增加 evidence 校验、confidence 拒绝和 metrics 输出。

4. 同步数据库设计和项目 README
   - 状态：Done
   - 备注：数据库设计新增 `tagging_attempts`、`suggested_new_tags`、`tag_review_queue`；README 增加运行契约、dry run、failed / needs_review 查询、taxonomy 扩展和失败类型说明。

5. 更新过程文档
   - 状态：Done
   - 备注：更新 `需求变更日志.md`、`系统结构.md`、`测试记录.md`。

6. 标签字典 v3 重构
   - 状态：Done
   - 备注：根据用户反馈，将字典改为基础数据字段 + Agent 判断标签两层结构；用 MECE 维度替换旧的 `topic` / `content_format` / `benefit_claim` 等重叠字段；公司改为自由实体抽取，不再预设 watchlist。

7. 同步 newsletter-tagging v3
   - 状态：Done
   - 备注：同步更新 `newsletter-tagging` 的参考字典、prompt、workflow、schema 和 validator；schema 版本升级到 `newsletter-tagging/v3`。

## 验证记录

| 检查/测试 | 命令或方式 | 结果 | 备注 |
|-----------|------------|------|------|
| JSON schema 解析 | `python3 -c 'import json; ...'` | Passed | `schema json OK` |
| 校验脚本语法 | `python3 -c 'import ast; ...'` | Passed | 避免生成 `__pycache__` |
| v2 合规样例 | `python3 -B -c` 导入 `validate_tags.py` 校验对象 | Passed | errors 为空，metrics 正常输出 |
| confidence 负例 | `python3 -B -c` 构造含 confidence 的 evidence | Passed | validator 返回 forbidden confidence |
| confidence 残留 | `rg` | Passed | 仅保留禁止/废弃 confidence 的规则说明 |
| v3 schema 解析 | `python3 -c 'import json; ...'` | Passed | `schema v3 json OK` |
| v3 validator 语法 | `python3 -c 'import ast; ...'` | Passed | 未生成 `__pycache__` |
| v3 合规样例 | `python3 -c` 导入 `validate_tags.py` 调用 `validate_item` | Passed | errors / warnings 为空 |
| 旧标签残留检索 | `rg` | Passed | 活跃运行文件已更新为 v3；历史变更记录保留旧口径说明 |

## 遇到的问题

| 问题 | 处理方式 | 状态 |
|------|----------|------|
| 旧字典和数据库设计中仍有 confidence / candidate_tags 口径 | 已同步改为 schema validation、evidence、review queue 和 suggested_new_tags | Resolved |
| 当前仍没有实际数据库 batch runner | README 和 workflow 记录 runner contract，并在 `dev/docs/问题跟踪.md` 新增 I-002 | Open |

## 关键决策

| 决策 | 依据 | 是否已写入需求变更日志 |
|------|------|------------------------|
| 废弃 AI 自评 confidence 作为入库依据 | 用户要求；confidence 不透明且不稳定 | 是 |
| 代码控制状态机、retry、validation、post-processing、写回和 metrics | 用户要求提升批量稳定性和可复核性 | 是 |
| LLM 只输出语义判断 JSON | 减少流程自由发挥，便于 schema 和脚本约束 | 是 |
| 公司名不进入字典，改为自由实体抽取 | 用户要求；公司字段用于直接入库和客户匹配，不适合作为永久 taxonomy | 是 |
| 标签体系分为基础字段和 Agent 判断字段 | 用户要求节省 token、提高流程稳定性和减少重复 | 是 |

## 下一步

1. 按 `references/workflow.md` 和 README contract 实现数据库 batch runner。
2. 建立真实 SQLite schema 和 `v_articles_for_tagging`。
3. 将 taxonomy 同步成结构化快照，供 runner 与 validator 读取。
4. 后续 runner 需要按 v3 schema 写入 `company` 自由实体，并在 post-processing 中做公司名归一化。

## 备注

本次主要是结构性 skill / schema / 文档改造，未实现实际数据库读写脚本。

---

## 追加会话（Codex）：禾大项目工作区重整

**工作阶段：** 从食品饮料模板迁移为禾大 Croda 情报订阅 Agent

### 完成

1. 读取项目规则、需求变更日志、问题跟踪、系统结构、项目管理文档、最近工作日志，以及禾大 outputs 和 prototype。
2. 解析 `reference/禾大newsletter/` 三份客户资料：
   - Word prompt：确认每月 1 日 09:00 运行、30 天回溯、公众号+官网抓取、交互式 HTML、双语、链接校验、深度分析和异常处理要求。
   - Excel：确认监测对象包括行业媒体、国内/国际原料商、国内代理商、MNC 客户品牌和国内品牌。
   - HTML prototype：确认月报栏目包括 TOP10、执行摘要、竞品动态、成分趋势、技术创新、市场活动、数据可视化和附录。
3. 清理目录：
   - `app/TradeMediaRSS/` 归档到 `reference/legacy-food-beverage/TradeMediaRSS/`。
   - 食品饮料旧字典和旧数据流说明归档到 `reference/legacy-food-beverage/`。
   - 删除 `dev/temp/pycache`、`reference/wewe-rss/node_modules`、`reference/wewe-rss/.git`、`.DS_Store`、`__pycache__`。
   - `reference/wewe-rss` 从约 797MB 降至约 3.4MB。
4. 重写活跃项目文档：
   - `README.md`
   - `app/README.md`
   - `dev/docs/项目管理文档.md`
   - `dev/docs/系统结构.md`
   - `dev/docs/数据库设计.md`
   - `dev/docs/里程碑检查清单.md`
   - `dev/docs/问题跟踪.md`
5. 将禾大版标签字典同步为当前活跃字典：
   - `dev/docs/标签字段字典.md`
   - `newsletter-tagging/references/标签字段字典.md`
6. 将 `newsletter-tagging` 同步为 Croda 版：
   - `SKILL.md`
   - `references/workflow.md`
   - `references/llm_prompt.md`
   - `schemas/tag_output.schema.json`
   - `scripts/validate_tags.py`
7. 修正活跃文档中误写的未来日期 `2026-06-04` 为当前日期 `2026-06-03`。

### 验证记录

| 检查/测试 | 命令或方式 | 结果 | 备注 |
|-----------|------------|------|------|
| Schema JSON | `python3 -c 'import json; ...'` | Passed | `schema json OK` |
| Validator 语法 | `python3 -c 'import ast; ...'` | Passed | `validator syntax OK` |
| Croda 合规样例 | 导入 `validate_tags.py` 调用 `validate_item` | Passed | 含 `other:synthetic_pdrn`，errors/warnings 为空 |
| 目录体积 | `du -sh` | Passed | `wewe-rss` 清理后约 3.4MB |

### 关键决策

| 决策 | 依据 |
|------|------|
| `app/` 清空为禾大正式代码区 | 旧 TradeMediaRSS 是食品饮料应用，不应作为当前交付代码 |
| 旧项目资产归档到 `reference/legacy-food-beverage/` | 保留可复用方法论和实现参考，同时避免污染活跃路径 |
| 禾大字典成为 `dev/docs/标签字段字典.md` 单一事实源 | 当前项目客户已切换为禾大 Croda |
| `newsletter-tagging` 使用 `newsletter-tagging/croda-beauty-v1` | 后续 Agent 执行必须按禾大字段和词表校验 |
| `ingredient_technology` 支持 `other:<slug>` 实时打标 | 继承用户确认的“宁多勿漏”和频次驱动晋升原则 |

### 下一步

1. 将 Excel 监测对象转为 `source_profiles` 和 `watchlist_entities` 配置。
2. 实现 SQLite schema 和 `v_articles_for_tagging`。
3. 实现 `run_tagging_batch.py` dry-run 与正式写回。
4. 基于 HTML prototype 实现月报生成 MVP。

---

## 追加会话（Claude）：禾大 Croda 美妆个护客户字典定制

**工作阶段：** 客户需求调研 + 定制标签字典

### 完成

1. 通读项目背景（README/AGENTS/SKILL/workflow/字典/需求变更日志 001-009/历次 work-log），梳理 tagging 定义、两层 MECE 实现逻辑、newsletter 流程，以及历次字典更新需求变更的方法论。
2. 调研 `reference/禾大newsletter/` 三份原型：确认禾大 = Croda（美妆个护原料商），与现食品饮料字典属不同行业。
3. 新增 `outputs/禾大Croda/禾大Croda-Newsletter需求分析报告.md`（监测对象/内容 insight/业务需求/原型功能/继承差异/风险）。
4. 新增 `outputs/禾大Croda/标签字段字典-禾大美妆个护版.md`（schema `newsletter-tagging/croda-beauty-v1`），在两层 MECE 架构上重构美妆个护词表，新增 `ingredient_technology`、`functional_claim`、`entity_role` 三个客户关键维度。
5. 更新 `需求变更日志.md`（新增 010，版本 v1.7）。

### 关键决策

| 决策 | 依据 |
|------|------|
| 沿用父项目两层 MECE/证据驱动/活字典/公司自由实体/无 confidence | 保证可维护性与项目一致性 |
| Agent 判断词表全行业本地化为美妆个护 | 禾大与食品饮料属不同行业，词表不可直接复用 |
| 新增 ingredient_technology 作为主轴+活字典每两周复核 | 月报核心是成分趋势+技术创新，新成分迭代快 |
| 公司名单作客户 watchlist 配置 + entity_role 受控维度 | 遵循决策 009 公司不入字典，同时满足角色筛选 |
| 打标不输出 confidence，置信度由生成阶段派生/人工复核 | 遵循决策 008 |

### 待确认（见需求分析报告 §7）

置信度展示口径、watchlist 同步、活字典节奏、下游品牌新闻相关性边界。

---

## 追加修订（Claude）：禾大字典 v1.1 去重 + 实时活字典

**触发：** 用户反馈两点。

1. **entity_role / value_chain_stage 去重**：确认二者在"产业链位置"上重复。将 `entity_role` 收敛为纯"关系轴"（self/competitor/customer/channel_partner/ecosystem），位置交回 value_chain_stage、地理交给 market_region、公司交给 company；附旧值迁移对照与月报分类还原说明。改 §1.2、§4.7、§4.8、§6.1、§6.2、§7 及需求报告对应处。
2. **ingredient_technology 内联 other 实时活字典**：新增 §4.4.1。字典外成分/技术由 Agent 实时写 `other:<slug>` + extracted_name + 证据，文章照常 tagged、即时可检索；频次驱动晋升，人工只看高频列表批量审核、不重读文章。原则"宁多勿漏"。§5 改为双路径活字典，§5.2 示例改 strategic_driver，§6.1/6.2/6.3 同步，需求报告风险 #3 更新。

**记录：** 需求变更日志新增 011（版本 v1.8）。字典版本号 → croda-beauty-2026-06-04 (v1.1)，schema 名保持 croda-beauty-v1。
