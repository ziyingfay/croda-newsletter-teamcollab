# 工作日志 - 2026-06-10

**工作阶段：** Croda Project Management Agent 目录迁移  
**执行人：** Codex

---

## 本次会话：迁入 croda-template 管理骨架

### 完成

1. 按旧项目规则读取 `需求变更日志.md`、`问题跟踪.md`、`测试记录.md` 和最新工作日志。
2. 读取用户提供的 `/Users/fangziying/Documents/项目管理agent/croda-template/`：
   - `AGENTS.md`
   - `README.md`
   - `project-management/skills/active/croda-project-management/SKILL.md`
   - directory/output rules
   - `.github/scripts/validate_structure.py`
3. 迁入新管理骨架：
   - 根级 `AGENTS.md`、`README.md`、`CONTRIBUTING.md`
   - `.github/scripts/validate_structure.py`
   - `.github/workflows/validate-structure.yml`
   - `product/`
   - `project-management/`
   - `project-management/skills/active/croda-project-management/`
4. 按新目录整理旧文件，未删除文件：
   - 旧 `newsletter-tagging/` → `product/skills/newsletter-tagging/`
   - 旧 `dev/docs/` → `project-management/dev/docs/`
   - 旧 `dev/notes/work-log-*.md` → `project-management/dev/notes/work-log/codex/YYYYMMDD.md`
   - 旧 `reference/禾大newsletter/` → `project-management/reference/source-materials/croda-newsletter/`
   - 旧 `reference/wewe-rss/` → `project-management/reference/examples/wewe-rss/`
   - 旧 `reference/legacy-food-beverage/` → `project-management/reference/archive/legacy-food-beverage/`
   - 旧 `outputs/禾大Croda/` 中配置类文件 → `product/parameter-file/`
   - 旧 demo、proposal、fixture → `product/output/`
   - 旧空目录和旧说明文件 → `project-management/reference/archive/`
5. 新增 `project-management/dev/docs/目录整理说明.md`，记录每个主要目录应包含哪些文件。
6. 同步活跃文档和 active skill 中的关键路径：
   - `newsletter-tagging` MVP 输入改为 `product/output/待打标.json`
   - spider archive 改为 `product/output/result/spider/<YYYYMM>/<timestamp>-rss-clean.json`
   - tagging 输出改为 `product/output/result/tagging/<YYYYMM>/tagging.json`
   - report 输出改为 `product/output/result/report/<YYYYMM>/`
7. 更新 `需求变更日志.md` 新增 022，更新 `测试记录.md` 新增 T-021。

### 校验

1. `python .github/scripts/validate_structure.py`：Passed。
2. `python -m json.tool product/parameter-file/source-profiles/source_profiles_seed.json`：Passed。
3. `python -m json.tool` 校验 3 份 HTML fixture：Passed。
4. `python -m py_compile .github/scripts/validate_structure.py`：Passed。
5. `python -m py_compile product/skills/newsletter-tagging/scripts/validate_tags.py`：Passed。

### 备注

- 本轮按用户要求没有执行删除操作；旧目录空壳也迁入 archive 保留。
- Git status 会显示大量 rename/delete/add，是目录移动造成的索引表现，不代表文件被清理丢弃。

---

## 追加会话：纠正产品交付 skill 放置位置

### 背景

用户澄清：`newsletter-tagging` 是客户交付产品流程中的“小龙虾” skill，客户侧需要安装并让它读取该 skill 生成打标结果和后续报告，不是内部项目管理 skill。

### 完成

1. 将 `newsletter-tagging` 从 `project-management/skills/active/newsletter-tagging/` 移到 `product/skills/newsletter-tagging/`。
2. 新增 `product/skills/README.md`，说明这里存放客户交付的 Xiaolongxia/OpenClaw skills。
3. 更新规则来源，避免再次误判：
   - `AGENTS.md` 新增 Skill Placement Rule。
   - `project-management/skills/active/croda-project-management/SKILL.md` 明确 `product/skills/` 与 `project-management/skills/` 的边界。
   - `project-management/skills/active/croda-project-management/references/directory-and-output-rules.md` 补充同样规则。
   - `product/README.md` 改为允许客户交付/产品流程 skill 放入 `product/skills/`。
4. 更新 `.github/scripts/validate_structure.py`：
   - 要求 `product/skills/newsletter-tagging/` 存在。
   - 禁止 `project-management/skills/active/newsletter-tagging/` 回流。
   - 将 `product/skills/` 纳入 Python-only backend/script 检查区域。
5. 更新活跃文档引用：
   - `project-management/dev/docs/目录整理说明.md`
   - `project-management/dev/docs/系统结构.md`
   - `project-management/dev/docs/MVP-Agent开发指引.md`
   - `project-management/dev/docs/MVP关键工件契约.md`
6. 更新单一事实源：
   - `project-management/dev/docs/需求变更日志.md` 新增 023。
   - `project-management/dev/docs/测试记录.md` 新增 T-022。

### 校验

1. `python .github/scripts/validate_structure.py`：Passed。
2. `python -m py_compile .github/scripts/validate_structure.py product/skills/newsletter-tagging/scripts/validate_tags.py`：Passed。

### 判断

本次问题来自规则表述不够精确：此前写的是 “Agent skills / reusable skill deliverables” 放在 `project-management/skills/`，没有说明客户交付、客户安装或产品流程读取的 skill 应归入 `product/skills/`。现已在 AGENTS、Project Management skill 和 directory rules 中明确区分。
