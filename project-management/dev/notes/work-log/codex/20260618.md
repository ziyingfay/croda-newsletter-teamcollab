# 工作日志 - 2026-06-18

**工作阶段：** 本地未记录变更补账与项目管理文档同步  
**执行人：** Codex

---

## 背景

用户说明同事在本地做了较多测试和修改，但没有按 Croda Project Management Agent 要求更新日志。目标是从上一次 Codex 记录后，读取当前文件夹变更并补齐相应项目管理文档。

## 盘点到的主要变更

1. HTML demo 继续迭代：
   - 6/13 生成并多轮修改客户可见 HTML demo。
   - 6/14 标签筛选改为 hover 右展级联菜单。
   - 202606 demo 后续迁入 `project-management/dev/docs/report/202606/html-demo/`，并拆成 `report-internal.html` 与 `report-final.html`，由 `render_report.py` 基于 `report_content.json` 生成。
2. V4 栏目体系继续收敛：
   - 正式打标栏目保留 5 类：竞品动态监测、新成分&新趋势情报、KA 监测、市场活动汇总、法规新政策。
   - 热点新闻 / 行业新闻快讯归为报告 Agent 编排层，不进入小龙虾 1 输出。
3. 栏目打标实验材料新增：
   - 文档区：`project-management/dev/docs/section-tagging-test/`。
   - 测试结果区：`product/output/test-results/section-tagging-test/`。
   - 样本参考：`project-management/reference/examples/all-articles(1).json`。
4. 新增 script-tagger 模块 B：
   - `product/script-tagger/script_tagger.py`
   - `product/script-tagger/validate_article_package.py`
   - `product/parameter-file/watchlist/`
   - `product/parameter-file/ingredient-alias/`
   - `product/parameter-file/event-keywords/`
   - 产物：`product/output/result/tagging/待打标.json` 与 `run_log.json`。
5. 旧输出和旧规划文件发生迁移：
   - MVP 规划三件套迁入 `project-management/dev/docs/mvp/`。
   - HTML demo / proposal 评审材料迁入 `project-management/dev/docs/report/` 或 `project-management/reference/archive/`。
   - 原 `product/output/result/report/` 被迁空，导致结构 validator 一度失败。

## 文档更新

1. `project-management/dev/docs/需求变更日志.md`
   - 新增版本 v3.0。
   - 新增 033：script-tagger 与配置 seed。
   - 新增 034：栏目打标实验材料归位。
   - 新增 035：HTML demo 评审材料归档与 report 运行骨架恢复。
2. `project-management/dev/docs/问题跟踪.md`
   - 新增 C-017：script-tagger 尚未接入 source_profiles / 真实 cleaner。
   - 新增 C-018：`待打标.json` 输出路径与固定交接路径待对齐。
   - 新增 C-019：参考工程本地出现 ignored `node_modules` 缓存。
   - 新增 C-020：report 空目录导致结构校验失败，已通过 `.gitkeep` 修复。
3. `project-management/dev/docs/测试记录.md`
   - 补齐 T-026 到 T-031 索引。
   - 新增 T-032，记录本次补账验证。
4. `project-management/dev/docs/目录整理说明.md`
   - 补充 `product/script-tagger/`、watchlist、ingredient alias、event keywords、section-tagging-test、report docs 等目录职责。
5. `project-management/dev/docs/系统结构.md`
   - 同步 script-tagger、V4 标签体系、HTML demo 评审区、栏目打标测试区和新的 MVP 数据流。
6. `project-management/dev/docs/里程碑检查清单.md`
   - 将 M2/M3/M4/M6 从旧的 Not Started/Pending 状态调整为当前 In Progress 状态，并区分 demo / prototype 与正式 MVP 交付。

## 修复

1. 新增 `product/output/result/report/.gitkeep`，恢复 Croda validator 要求的运行期 report 标准目录。

## Verification

1. `python3 .github/scripts/validate_structure.py`：首次失败，缺 `product/output/result/report`；补 `.gitkeep` 后 Passed。
2. `git diff --check`：Passed。
3. `PYTHONPYCACHEPREFIX=/private/tmp/codex-pycache python3 -m py_compile ...`：Passed。
4. JSON 解析：watchlist、ingredient alias、event keywords、`待打标.json`、run log、HTML demo `report_content.json` 均 Passed。
5. `python3 product/parameter-file/watchlist/validate_watchlist.py product/parameter-file/watchlist/watchlist_entities_seed.json`：Passed；73 entities、178 aliases、10 个英文别名待运营补全。
6. `python3 product/script-tagger/validate_article_package.py product/output/result/tagging/待打标.json`：Passed；450 条记录，396 success、54 partial，company 266、ingredient 330、is_event 96。

## Remaining Risks

1. `product/output/result/tagging/待打标.json` 需要与 AGENTS 中固定交接路径 `product/output/待打标.json` 对齐。
2. script-tagger 当前基于已抽正文样本，不代表正式 spider / cleaner 端到端已跑通。
3. `project-management/reference/examples/wewe-rss/` 下本地出现 ignored `node_modules` 缓存；正式交接或打包前需清理或确认忽略策略。
